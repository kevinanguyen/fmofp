# FMOFP B20SS — Planning & Progress Document

> **Last updated:** August 21, 2026 (independent live-boot re-verification — see the
> boxed note at the top of Known Issues: the app had been unable to boot since Aug 12
> despite green CI; four boot-path defects fixed, boot now verified end-to-end, boot
> smoke test + full-suite runner added to CI. Repo also gained README, LICENSE,
> `FMOFP.__version__` = 1.1.0, and a reconciled Bitbucket pipeline.)
>
> **Correction (August 2026):** the "Stub / Incomplete Subsystems" table below was inaccurate. Defensive Systems, Sensor Management, Mission Planning, and Avionics Hardware Health were all already substantially implemented and wired into `system_manager.py` as of this doc's own June 2026 "last updated" date — the doc simply wasn't regenerated to reflect that. An August 2026 audit read and ran each of the four live, found and fixed four real bugs in the process (see Known Issues), and confirmed all four are genuinely functional, not stubs. Moved to "Fully Operational" below.

---

## 1. Project Overview

The **Flight Management Operating Flight Program (FMOFP)** is a Python-based avionics simulation system for the B20SS military aircraft. It integrates five radar types, three display families, a full MIL-STD-1553B bus simulation, flight management, flight control, and a suite of aircraft sub-systems through an event-driven, async/threading architecture.

**Runtime stack:** Python 3.9+, PyQt6, NumPy, SQLite (via internal `DBM`), `asyncio` + `qasync`
**Target OS:** Windows 10/11 64-bit (PyQt6 wheels bundled for AMD64 and ARM64)
**Entry point:** `B20SS/FMOFP/Main.py`

---

## 2. System Architecture

### 2.1 Layer Model

```
┌─────────────────────────────────────────────────────────────────────┐
│                        User Interface Layer                         │
│  PFD / Futuristic-PFD / Holographic-PFD                            │
│  MFD / Futuristic-MFD / Holographic-MFD                            │
│  Radar displays (Weather, Targeting, SAR, TFR, AEWC)               │
│  EICAS · TSD · SMS · HUD container                                  │
└─────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────┐
│                       Application Layer                             │
│  SystemManager · Initializer · EventBus                             │
│  RadarManagement · DisplayManagement · FlightManagementSys          │
│  FlightControlSys · NavigationSys · CommsSys · MissionPlanning      │
└─────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────┐
│                     Communication Layer                             │
│  MIL-STD-1553B (Bus Controller + Remote Terminals)                  │
│  LocalMessaging routing stack (unified router, response services)   │
│  Radar-to-Display Bridge (synchronous short-circuit path)           │
└─────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────┐
│                         Data Layer                                  │
│  DBM (SQLite wrapper) · 7 specialised databases · schema.xml        │
│  RadarDisplayDataCoordinator (TTL-backed in-memory store)           │
│  RadarDataFusion (cross-radar threat correlation, singleton)        │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 Directory Structure

```
B20SS/
├── FMOFP/
│   ├── Main.py / SystemStart.py          Entry points
│   ├── core/                             SystemManager, EventBus, Initializer
│   ├── Systems/
│   │   ├── radarManagement/              5 radar types + fusion layer
│   │   ├── flightManagementSys/          FMS + FMS messaging
│   │   ├── flightControlSys/             FCC + GCAS + performance monitoring
│   │   ├── nav/                          GPS, INS, TACAN, data fusion
│   │   ├── comms/                        Radios, SatCom, data link
│   │   ├── missionPlanning/              Route mgmt, order of battle, targeting
│   │   ├── avionics/                     Hardware health monitoring
│   │   ├── defensiveSys/                 RWR, countermeasures, EW (see Known Issues, Aug 2026 audit)
│   │   ├── sensorManagement/             Active/passive sensors + fusion (see Known Issues, Aug 2026 audit)
│   │   └── [9 other subsystems]          airframe, electrical, engine, env, etc.
│   ├── Interfaces/
│   │   ├── userInterface/displays/       All Qt display classes + visual layer
│   │   ├── predefinedMessages/           Typed message classes for all 5 radars
│   │   └── scenarios/                   Training / failure XML scenarios
│   ├── MIL_STD_1553B/                    Full 1553B protocol implementation
│   ├── local_messaging/                  Routing, handlers, response services
│   ├── storage/                          DBM.py, schema.xml
│   ├── Utils/                            Logger, thread mgr, loop prevention, CLI
│   └── Tests/                           Integration + unit test suites
├── FMOFP_User_Manual/                   14-chapter markdown manual
├── __ABOUT__/                           Architecture diagrams, screenshots, demos
├── __Diagrams__/                        UML package/component/sequence/state PNGs
├── __TOOLS__/                           Dev utilities (cleanup, XML naming, struct)
├── PyQt6/                              Bundled wheels (AMD64 + ARM64)
├── python packages/                    Additional bundled packages
└── install.py                          6-step automated installer
```

### 2.3 MIL-STD-1553B Implementation

**Status: ✅ OPERATIONAL**

Full simulation of the military avionics data bus with:
- 16-bit Command / Status / Data word encoding and validation
- Bus Controller (BC) + Remote Terminal (RT) architecture
- Block transfer manager for large data sets
- Message loop prevention (UUID tracking + middleware decorators)
- Transaction tracking via `tracking_library.py`
- Metadata codec and message schema normalisation

**RT Address assignments (from `rtAddressConfig.xml`):**

| RT | System |
|----|--------|
| 5 | Flight Control System |
| 7 | Navigation Systems |
| 9 | Radar Systems |
| 11 | Display Systems |
| 12 | Flight Management System |

**Radar subaddresses (RT 9):**

| Sub | Radar |
|-----|-------|
| 1 | Weather |
| 2 | TFR |
| 3 | SAR |
| 4 | Targeting |
| 5 | AEWC |

**Display subaddresses (RT 11):** PFD (11), MFD (12), EICAS (13), Radar (14), TSD (15), SMS (16)

### 2.4 Local Messaging / Routing Stack

**Status: ✅ OPERATIONAL**

The `local_messaging/` layer is the internal pub-sub backbone that decouples radar data generation from display updates:

- `UnifiedRouter` → `MessageRoutingService` → `MessageDispatcher`
- `RouteResolver` + `RoutingRegistry` for type-based dispatch
- `MessageValidator` + `MessageTransformer` for normalisation
- Handler hierarchy: `BaseMessageHandler` → `RadarMessageHandler`, `FCSMessageHandler`, `FMSMessageHandler`, `DisplayMessageHandler`
- Response service hierarchy: `RadarResponseService`, `FMSResponseService`, `DisplayResponseService`
- Data response services: VIL, Precipitation, EchoTop, FMS attitude/nav/tactical/velocity
- `AsyncMessageHandler` (4-worker thread pool, UUID tracking, rate limiting 10 req/s)
- `DisplayOutgoingRouter` for display → radar direction

**Key resolved issue — Radar-to-Display Bridge:**

The original flow attempted to push data through the RT → BC direction of the 1553B bus, which created two problems:
1. Data was sent *toward* the Bus Controller, never reaching the display coordinator
2. VILResponseService → DisplayMessageHandler → MessageRoutingService → VILResponseService formed a message loop silently killed by loop-prevention decorators

**Resolution:** `radar_to_display_bridge.py` short-circuits this by importing `RadarDisplayDataCoordinator` directly. It is framework-agnostic (no asyncio, no Qt) and callable from any thread. All five radar types now push through this bridge.

**Note (August 2026):** the bridge path above is separate from, and was not affected by, the `RadarMessageHandler` / `AsyncMessageHandler` local-messaging pub-sub path also described on this page — the latter was found to be completely broken for TFR/SAR/targeting/AEWC (see Known Issues: "Non-weather radar message classes could not be constructed at all"). The bridge is what actually gets live radar data onto the display for all five radar types today; the pub-sub path is a parallel mechanism used for internal bus-message construction and had never been live-exercised for the four non-weather radars until this audit.

### 2.5 Radar Display Data Coordinator

**Status: ✅ OPERATIONAL**

`RadarDisplayDataCoordinator` (singleton) provides TTL-backed in-memory storage between radar processing cycles and Qt paint events:

- Three data stores: `precipitation`, `vil`, `cells` — each with `current` + `backup` + 5s TTL
- `store_data(data_type, items, request_id)` / `get_data(data_type, use_backup=True)`
- Backup fallback: if `current` is expired, serves `backup` transparently
- `cleanup_expired()` runs on a 5s interval
- `_process_items()` normalises typed objects (VILData, PrecipitationData, dicts) to a uniform dict format
- Thread-safe with throttled logging (10s interval)

The bridge + coordinator combination is covered by 15 unit tests in `test_bridge_and_coordinator.py`.

### 2.6 Cross-Radar Data Fusion

**Status: ✅ OPERATIONAL**

`RadarDataFusion` (singleton, `radar_data_fusion.py`) correlates tracks from all five radar systems into a unified `FusedTrack` list:

- Ingests targeting, AEWC, SAR, TFR, weather data
- Computes range, bearing, altitude from position vectors
- Classifies tracks: HOSTILE / FRIENDLY / UNKNOWN based on identity/classification fields
- Exports `to_tsd_dict()` for direct TSD rendering
- Thread-safe with lock on `__new__`

---

## 3. Radar System Implementations

### 3.1 Weather Radar ✅ OPERATIONAL

**Files:** `Systems/radarManagement/weather/`

| Component | File | Status |
|-----------|------|--------|
| Core radar class | `weather_radar.py` | ✅ |
| VIL data generator | `vil_data_generator.py` / `_sync.py` | ✅ |
| Precipitation generator | `precipitation_data_generator_sync.py` | ✅ |
| Reflectivity simulator | `reflectivity_simulator.py` | ✅ |
| Storm cell tracker | `radar_messaging/stormCellTracking.py` | ✅ |
| Precipitation analyser | `radar_messaging/precipitation_analysis.py` | ✅ |
| Wind shear processor | `weather_processor.py → WindShearProcessor` | ✅ |
| Turbulence processor | `weather_processor.py → TurbulenceProcessor` | ✅ |
| Weather state manager | `weather_state_manager.py` | ✅ |
| Message type detector | `weather_message_type_detector.py` | ✅ |
| Simulated radar | `SimulatedWeatherRadar.py` | ✅ |

**Modes:** STANDBY · SURVEILLANCE · MAPPING · TURBULENCE · WINDSHEAR

**Capability detail:**
- `StormCellTracker`: linked-list of `StormCell` dataclasses (position, altitude, reflectivity, velocity, size, intensity, vertical development, timestamp)
- `PrecipitationAnalyzer`: dBZ thresholding (≥30 dBZ), Z-R relationship for rate, type classification (rain/snow/hail/mixed)
- `WindShearProcessor`: radial velocity divergence detection, microburst flagging, severity levels LOW/MODERATE/SEVERE
- `TurbulenceProcessor`: EDR proxy from spatial reflectivity variance, FAA AC 120-88A categories LIGHT/MODERATE/SEVERE/EXTREME
- VIL: layer integration of reflectivity, vertical profile, storm intensity
- Update rate 1 Hz min; range 50/100/200 nm; elevation −15° to +90°; azimuth resolution 0.5°

**Display path:** `weather_radar.py` → `radar_to_display_bridge.push_vil_data / push_precipitation_data` → `RadarDisplayDataCoordinator` → `WeatherRadarDisplay` / `HolographicWeatherRadarDisplay`

**Known issue:** Weather radar → display data flow via the 1553B chain is still not fully operational. The bridge path is the current working route.

---

### 3.2 Targeting Radar ✅ OPERATIONAL

**Files:** `Systems/radarManagement/targeting/`

| Component | File | Status |
|-----------|------|--------|
| Core radar class | `targeting_radar.py` | ✅ |
| Target processor | `target_processor.py` | ✅ |

**Modes:** STANDBY · SEARCH · TRACK · LOCK · GROUND_MAPPING · TERRAIN_AVOIDANCE

**Capabilities:** Multi-target tracking (100+ targets), track-while-scan, RCS signature analysis, ECM/ECCM, target classification

**Technical requirements:** 10 Hz update, 5 m range precision, 1 m/s velocity accuracy

**Display path:** `targeting_radar.py` → `radar_to_display_bridge.push_targeting_data` → coordinator → `TargetingRadarDisplay`

---

### 3.3 SAR Radar ✅ OPERATIONAL

**Files:** `Systems/radarManagement/syntheticAperture/`

| Component | File | Status |
|-----------|------|--------|
| Core radar class | `sar_radar.py` | ✅ |
| SAR processor | `sar_processor.py` | ✅ |

**Modes:** STANDBY · STRIPMAP · SPOTLIGHT · SCANSAR · INTERFEROMETRIC · DOPPLER_BEAM

**Capabilities:** 0.3 m resolution (spotlight), 10 km swath, <1 s processing latency, 3D terrain reconstruction via interferometry, change detection

**Display path:** `radar_to_display_bridge.push_sar_data` → coordinator → `SARRadarDisplay`

---

### 3.4 AEWC Radar ✅ OPERATIONAL

**Files:** `Systems/radarManagement/aewc/`

| Component | File | Status |
|-----------|------|--------|
| Core radar class | `aewc_radar.py` | ✅ |
| AEWC processor | `aewc_processor.py` | ✅ |

**Modes:** STANDBY · SEARCH · TRACK · SECTOR_SCAN · GROUND_MAPPING · STEALTH_DETECTION · ELECTRONIC_PROTECTION

**Capabilities:** 200+ nm range, 1000+ target capacity, stealth track detection, electronic protection (active/passive jamming mitigation), data fusion, formation analysis

**Technical requirements:** 6 rpm minimum rotation, false alarm rate <10⁻⁶

**Display path:** `radar_to_display_bridge.push_aewc_data` → coordinator → `AEWCRadarDisplay`

---

### 3.5 TFR Radar ✅ OPERATIONAL

**Files:** `Systems/radarManagement/terrainFollowing/`

| Component | File | Status |
|-----------|------|--------|
| Core radar class | `tfr_radar.py` | ✅ |
| TFR processor | `tfr_processor.py` | ✅ |
| Message type detector | `tfr_message_type_detector.py` | ✅ |

**Modes:** STANDBY · SEARCH · TRACK · TERRAIN_FOLLOWING · OBSTACLE_AVOIDANCE · GROUND_MAPPING

**Capabilities:** Terrain profile matching, obstacle detection (linked-list of obstacles with range/elevation/classification/threat), dynamic path optimisation, wire detection, ground mapping

**Technical requirements:** 20 Hz update, 1 m range resolution, 0.5 m height accuracy, 5 nm look-ahead minimum

**Display path:** `radar_to_display_bridge.push_tfr_data` → coordinator → `TFRRadarDisplay`

---

## 4. Display System

### 4.1 Display Class Hierarchy ✅ OPERATIONAL

```
BaseDisplay (base_display.py)
├── PFD (pfd.py)
│   ├── FuturisticPFD (futuristic_pfd.py)
│   └── HolographicPFD (holographic_pfd.py)
├── MFD (mfd.py)
│   ├── FuturisticMFD (futuristic_mfd.py)
│   └── HolographicMFD (holographic_mfd.py)
├── HolographicDisplay (holographic_display.py)
├── EICASDisplay (eicas.py)              — engine/system alerts
├── TacticalSituationDisplay (tsd.py)   — threat picture, fused tracks
└── StoresManagementDisplay (sms.py)    — weapon station monitoring

BaseRadarDisplay (radar/base_radar_display.py)
├── FuturisticRadarDisplay (futuristic_radar_display.py)
│   └── HolographicRadarDisplay (holographic_radar_display.py)
├── WeatherRadarDisplay (weather_radar_display.py)
│   └── HolographicWeatherRadarDisplay (weather_radar_holographic_display.py)
├── TargetingRadarDisplay (targeting_radar_display.py)
├── SARRadarDisplay (sar_radar_display.py)
├── TFRRadarDisplay (tfr_radar_display.py)
└── AEWCRadarDisplay (aewc_radar_display.py)

Containers:
├── HUDContainer (hud_container.py)
├── PFDContainer (pfd_container.py)
└── MFDContainer (mfd_container.py)
```

### 4.2 Visual / Theme Layer ✅ OPERATIONAL

```
Interfaces/userInterface/displays/visual/
├── theme_manager.py           — DisplayTheme enum (CLASSIC, MODERN, MINIMAL)
├── enhanced_theme_manager.py  — EnhancedDisplayTheme (STANDARD, HOLOGRAPHIC, FUTURISTIC)
├── effects.py                 — VisualEffects base class
├── enhanced_effects.py        — EnhancedVisualEffects (parallax, depth, glow)
├── animation_controller.py    — AnimationController + TransitionGroup (QObject-based)
├── settings_panel.py          — SettingsPanel + SettingsOption
├── holographic_settings_panel.py — Extended holographic controls
└── theme_config.json          — Persisted theme preferences
```

### 4.3 Display Node System ✅ OPERATIONAL

A state-tree system for managing display modes:

```
display_nodes/
├── display_node_base.py   — DisplayNode + NodeMetadata base
├── visual_node.py         — VisualNode (rendering state)
├── orientation_node.py    — OrientationNode (attitude / heading)
├── mode_node.py           — ModeNode (radar mode → display state mapping)
└── display_tree_manager.py — DisplayTreeManager + FallbackMode enum
```

`DisplayTreeManager` dynamically loads radar enums from module introspection and maps them to display state nodes with fallback handling.

### 4.4 Enhanced Radar Rendering System ✅ OPERATIONAL

```
Interfaces/userInterface/displays/radar/rendering/
├── radar_rendering_engine.py     — Core rendering with Gaussian kernel blobs
├── particle_system.py            — Particle-based weather visualisation
├── particle_renderer.py          — Integrates particle system with rendering engine
├── animation_controller.py       — Wind vectors, temporal effects, frame timing
├── spatial_partitioning.py       — Cell-based culling for performance
├── weather_data_buffer_manager.py — Multi-layer buffer management
├── enhanced_radar_display.py     — Drop-in wrapper: EnhancedRadarDisplay(existing_display)
└── USAGE_GUIDE.md                — Integration guide with code examples
```

**Key features:**
- Gaussian kernel rendering: smooth blob-like weather returns replacing simple geometry
- Particle renderer: wind-driven particle clusters with lifetimes, clustering algorithm (main + satellite clusters with normal-distribution spread)
- Quality levels 1–5: controls kernel size, particle count, noise, animation complexity
- `EnhancedRadarDisplay(display)` wraps any existing display instance with automatic legacy fallback
- Wind parameters: `set_wind_parameters(direction_deg, speed_px_s)` + `set_turbulence(0.0–1.0)`
- Animation via `@keyframes`-equivalent using `AnimationController` + `QTimer`

### 4.5 Radar Event System ✅ OPERATIONAL

```
radar/radar_event_system/
├── radar_event_manager.py   — Publish/subscribe for radar state changes
├── radar_topic_registry.py  — Topic name → handler registry
└── display_cache.py         — Short-lived display-side data cache
```

### 4.6 Display Factory Pattern ✅ OPERATIONAL

| Factory | File |
|---------|------|
| `HUDDisplayFactory` | `hud_display_factory.py` |
| `MFDDisplayFactory` | `mfd_display_factory.py` |
| `PFDDisplayFactory` | `pfd_display_factory.py` |
| `RadarDisplayFactory` | `radar/radar_display_factory.py` |

---

## 5. Flight Management System (FMS) ✅ OPERATIONAL

```
Systems/flightManagementSys/
├── flightManagementSystem.py   — Core FMS
├── fmsControl.py               — Control interface
├── fmsMessenger.py             — 1553B messaging
├── fms_message_processor.py    — Incoming message handling
├── fms_message_type_detector.py
└── fms_messaging/              — Response services (attitude, nav, velocity, tactical)
```

FMS provides real-time flight data to the PFD/MFD:
- Attitude (pitch, roll, yaw, G-force, AoA)
- Navigation (position, waypoints, flight plan)
- Velocity (airspeed, groundspeed, vertical speed)
- Tactical (energy state, threat awareness)

Response services: `FMSAttitudeResponseService`, `FMSNavigationResponseService`, `FMSVelocityResponseService`, `FMSTacticalResponseService`

---

## 6. Flight Control System (FCS) ✅ OPERATIONAL

```
Systems/flightControlSys/
├── flightControlComputer/      — FCC implementation
├── groundCollisionAvoidanceSys/ — GCAS
└── performaneMonitoring/       — Performance envelope monitoring
```

FCS messages covered by `predefinedMessages/fcs_messages.py` and handled by `FCSMessageHandler` / `FCSResponseService`.

---

## 7. Navigation System ✅ OPERATIONAL (partial)

```
Systems/nav/
├── gps/           — GPS receiver simulation
├── ins/           — Inertial navigation system
├── TACAN/         — TACAN navigation aid (not referenced anywhere else in the codebase — dead/unwired)
└── dataFusion/    — GPS + INS fusion
```

**Verified live (August 2026):** `NavService` (`navService.py`) start → GPS thread produces a fix within ~1s of `FlightManagementSystem.start()` → `NavDataFusion` fuses it → `FMS.update_navigation()` receives it, all with zero errors. Note: `NavService` attaches to GPS/INS/fusion via `FMS.gps_system`/`FMS.nav_fusion` (`_lazy_init()`), and `GPSSystem.start_thread()` is only called from inside `FMS.start()` — a `NavService` tested in isolation without a started `FMS` will correctly show `gps_available: False`, that's not a bug, just means FMS hasn't been started yet.

---

## 8. Communications System ✅ OPERATIONAL (framework)

```
Systems/comms/
├── radios/           — NOT referenced anywhere else in the codebase — dead/unwired
├── satcom/           — NOT referenced anywhere else in the codebase — dead/unwired
├── dataLink/         — NOT referenced anywhere else in the codebase — dead/unwired
└── messaging_service.py   — the actual live implementation (CommsService / get_comms_service())
```

**Verified live (August 2026):** `CommsService`'s full control surface (radio frequency/mode/volume/squelch, satellite acquisition, radio/satcom/datalink transmit) and the live routing path that reaches it from real message traffic (`MessageRoutingService.route_comms_message()`, covering all 8 `comms_*` message types) both work correctly with zero errors. Note: `CommsMessageHandler` (`local_messaging/routing/handlers/system_message_handlers/CommsMessageHandler.py`) is constructed and `.start()`-ed by `system_manager.py` but its `handle_message()` is never actually called by anything — all real comms routing goes through `route_comms_message()` instead. Not a bug (the live path works), just means that class is currently dead code despite being wired into startup.

---

## 9. Mission Planning ✅ OPERATIONAL (framework)

```
Systems/missionPlanning/
├── missionControl.py
├── missionData/        — Mission data storage
├── orderOfBattle/      — OOB management
├── routeManagement/    — Waypoint / route handling
└── targeting/          — Target designation
```

**Verified live (August 2026):** all 20 public `MissionService` methods (phase control, waypoints, targeting, order of battle, ROE/objectives/intel, route optimisation) and the live routing path (`MessageRoutingService.route_mission_message()`, all 15 `mission_*` message types) exercised directly with realistic data — zero errors. Same dead-code note as Communications above: `MissionMessageHandler.handle_message()` is never actually invoked; `route_mission_message()` is the real path.

**Fixed (August 2026):** `MissionService._update()` synced `self._current_position` from `FMS.get_flight_data()['navigation']` using a bare `if nav:` truthiness check, but confirmed live that FMS always returns a fully-populated navigation dict — including before any real fix exists, as literal zeros (`{'latitude': 0, 'longitude': 0, ...}`), not `None`/empty. Since `MissionService`'s update loop's very first tick fires immediately with no initial delay, this unconditionally overwrote the sensible startup default (`{'lat': 35.4147, 'lon': -97.3866, 'alt_ft': 1290.6}`, set in `__init__`) with `(0, 0, 0)` — "null island" — the moment the loop started, regardless of whether FMS had produced real data yet. In the real boot sequence Mission Planning starts well after FMS, but with no guaranteed minimum gap, so this could transiently show an incorrect position on the mission/TSD display right after boot. Fixed by only syncing when FMS reports a non-placeholder fix (lat/lon not both exactly 0). Verified live both ways: default correctly preserved when FMS has no real fix yet; still syncs correctly once FMS/GPS are actually running. See commit `6e8fb8b`.

---

## 10. Predefined Message Library ✅ OPERATIONAL

```
Interfaces/predefinedMessages/
├── Messages.py                    — Registry / dispatcher
├── message_base.py               — Base message class
├── radar_enums.py                 — Shared radar enumerations
├── weather_radar_messages.py      — Typed weather message classes
├── targeting_radar_messages.py    — Targeting message classes
├── sar_radar_messages.py          — SAR message classes
├── tfr_radar_messages.py          — TFR message classes
├── aewc_radar_messages.py         — AEWC message classes
├── fms_messages.py                — FMS message classes
├── fcs_messages.py                — FCS message classes
└── usage_example.py               — Integration examples
```

---

## 11. Installer ✅ OPERATIONAL

`B20SS/install.py` — 6-step automated installer:

1. **Python version check** — requires 3.9+
2. **Directory structure** — validates all required subdirectories
3. **Dependencies** — installs PyQt6, NumPy, qasync from bundled wheels or PyPI; supports `--offline`, `--force-reinstall`
4. **Config validation** — parses and validates all four XML config files
5. **Database initialisation** — creates all SQLite tables from `schema.xml`
6. **Startup verification** — dry-runs all subsystem imports and confirms STANDBY state

Supports `--offline`, `--no-verify`, `--force-reinstall` flags. Exit codes: 0 = success, 1 = failure.

---

## 12. Test Coverage

| Test file | What it covers |
|-----------|----------------|
| `test_bridge_and_coordinator.py` | 15 tests: coordinator store/get/TTL/backup/reset + bridge push for all 5 radars |
| `radar_tests/weather_radar_test.py` | Weather radar mode transitions, VIL, precipitation |
| `radar_tests/targeting_radar_test.py` | Targeting modes, track lifecycle |
| `radar_tests/sar_radar_test.py` | SAR mode transitions |
| `radar_tests/tfr_radar_test.py` | TFR terrain following modes |
| `radar_tests/aewc_radar_test.py` | AEWC surveillance modes |
| `combined_precipitation_vil_flow_test.py` | End-to-end precipitation + VIL data flow |
| `weather_radar_surveillance_mode_test.py` | Surveillance mode data pipeline |
| `fms_system_test.py` | FMS message processing and response services |
| `flight_control_system_test.py` | FCS mode and message handling |
| `predefined_messages_test.py` | Message class construction and serialisation |
| `test_displays_headless.py` | EICAS, TSD, SMS display logic (headless Qt) |
| `test_weather_radar_holographic_display.py` | Holographic weather display rendering |
| `test_bridge_and_coordinator.py` | Bridge + coordinator (15 cases) |
| `test_install_script.py` | Installer step validation |
| `test_precipitation_data_transfer.py` | Precipitation data transfer pipeline |
| `test_power_fuel_thrust.py` | 43 tests: `PowerManagementSystem` (battery/cooling/HVAC construction, `adjust_power_parameters()` ranges, `monitor()`, the heat-load-scaling regression, start/stop lifecycle), `FuelSystem`/`FuelManagementSystem` (the missing-table regression, tank clamping, `setup_aircraft()`, `perform_fuel_transfer()`, the redundant-transfer-rule regression, `get_status()`, start/stop lifecycle), `ThrustManagementSystem` (register/set/get thrust, asymmetry check, autothrottle PI controller, start/stop lifecycle) |
| `test_hydr_airframe_ecs_fdm_fitness_swcm.py` | 47 tests: `HydraulicSystemController`, `AirframeSystemManager`, `ECSControl`, `FlightDataMonitoring`, `FlightManagementFitness` (config load, subsystem-specific behavior, start/stop lifecycle each) and `SoftwareConfigManager` (config load, `perform_update()` valid/invalid, singleton-with-no-background-thread) |
| `test_radar_shutdown_health.py` | 12 assertions: `RadarManagementSystem._update_loop()` distinguishes a normal shutdown from ill health — covers the flag clearing both between iterations and (deterministically) mid-iteration, that a genuinely unhealthy radar still logs its ERROR and clears `_running`, and that a healthy loop keeps running. Verified non-tautological: removing the guard fails 3 assertions. |
| `test_thread_and_cli_resilience.py` | 15 assertions: `ThreadManager.start_thread()`'s already-run guard (an is_alive()-only check let `.start()` raise "threads can only be started once" on a finished thread — the intermittent `UserCLI_Input`/`UserCLI_Processing` boot errors) and `UserCLI.get_commands()` per-iteration error containment plus its consecutive-failure circuit breaker. Verified non-tautological: the pre-fix path raises `RuntimeError` and would fail the "restart logs no ERROR" assertion. |
| `test_db_connection_pool.py` | 16 assertions: DBM connection-pool contention regression — grow-before-block ordering (the cold-start pool-exhaustion fix), concurrent burst within capacity, genuine saturation still raising `ConnectionPoolTimeout` and recovering on return, no `connection_count` leak when `create_connection()` fails, dead-connection replacement accounting, and a 40-thread × 15-op hammer. Verified non-tautological: restoring the original acquisition order makes the timing assertion fail (5.00s vs <0.01s). |
| `test_bus_adapter.py` | 24 assertions: MIL-STD-1553B bus adapter layer (factory defaults + config, socket adapter real-transmit and connection-refused contract, loopback delivery, hardware adapter driver contract/delegation/instructive errors, BC/RT sender integration through the adapter) |
| `test_scenario_failure_injection.py` | 29 assertions: forced-fault override in `LRUStatusMonitor` (immediate visibility, survival across poll cycles, clear/rederive, scenario-declared synthetic entries) and `ScenarioEngine._inject_failure` set/clear/clear_all/state routing, incl. every `failureScenario.xml` event end-to-end through the real dispatcher |
| `test_toctou_start_race_regression.py` | 18 tests: hammers `start()` concurrently (12 simultaneous callers via a `threading.Barrier`) on 9 of the singletons fixed for the August 2026 start() TOCTOU race (`ThrustManagementSystem`, `HydraulicSystemController`, `AirframeSystemManager`, `ECSControl`, `FlightManagementFitness`, `FlightDataMonitoring`, `PowerManagementSystem`, `FuelManagementSystem`, `NavService`) and asserts via `threading.enumerate()` that exactly one live thread with the expected name exists afterward. Verified live that this harness actually detects the race: temporarily monkey-patching `ThrustManagementSystem.start()` back to the original unguarded pattern and re-running the identical hammer produced 4 duplicate `TMS_Update` threads instead of 1, confirming the test isn't a tautology. |

**Test runner:** `Tests/setup_env.py` configures `sys.path`; all tests are standalone scripts using a lightweight `_Results` harness (no pytest dependency).

**Gap closed (August 2026, follow-up round).** The gap noted below has been addressed: `test_power_fuel_thrust.py`, `test_hydr_airframe_ecs_fdm_fitness_swcm.py`, and `test_toctou_start_race_regression.py` (108 test cases total) now cover every subsystem built out, fixed, or wired into the boot sequence this session, plus a dedicated concurrency regression guard for the start() race itself. All three are wired into `.github/workflows/ci.yml` alongside the existing suite. One constraint documented inline in `test_power_fuel_thrust.py`: `FuelSystem.__init__()` shares the `dbConfig.xml` `'default'` system database (no dedicated `'engine'` entry exists), which rate-limits `create` queries to 10 per 60s -- each `FuelSystem`/`FuelManagementSystem` construction consumes 3, so fuel-related tests are deliberately consolidated to minimize instantiations rather than isolated one-construction-per-check the way the rest of the suite is.

Original gap, for the record: none of the subsystems built out, fixed, or wired into the boot sequence over the last several rounds -- `PowerManagementSystem`, `FuelManagementSystem`/`FuelSystem`, `ThrustManagementSystem`, the battery classes (`MainBattery`/`EmergencyBattery`/`APUBattery`/`MissionEquipmentBattery`), the thermal classes (`CoolingSystem`/`HVACSystem`/`ActiveDissipationUnit`), `FuelMonitor`/`FuelTransferManager`, `HydraulicSystemController`, `AirframeSystemManager`, `ECSControl`, `FlightDataMonitoring`, `FlightManagementFitness`, `SoftwareConfigManager` -- had any automated test coverage in this suite; all verification had been live-exercised manually (construct, run, inspect logs/`get_status()`, stop) rather than captured as a repeatable, runnable test.

**Assertion-quality spot-check (August 2026, re-analysis round).** Audited the CI-wired suite plus the larger pre-existing (not-this-session) test files for tautological assertions, swallowed failures, and other false-positive risks -- an AST scan for test functions with zero `assert`/`self.assert*` calls initially flagged ~90 functions across nearly every file, but manual review showed these all use a custom `_Results.check(name, cond, detail)` harness (or, in the older integration-style files, `raise AssertionError`/`success_rate` dict patterns) rather than bare `assert`, and both patterns correctly track and propagate failures -- false alarm from the scan, not a real gap. The genuine finding from this pass was structural rather than about individual assertions: `predefined_messages_test.py` and `combined_precipitation_vil_flow_test.py` both computed real pass/fail state internally but then discarded or ignored it on their return path, so a caller could never observe a failure short of an unhandled exception. Fixed -- see Known Issues below.

---

## 13. Configuration Files

| File | Purpose |
|------|---------|
| `rtAddressConfig.xml` | RT address → system name mapping |
| `messageRateConfig.xml` | Per-message-type transmission rates and priorities |
| `queryRateConfig.xml` | Database query rate limits per system |
| `dbConfig.xml` | Database assignments, pool sizes, retry policies |
| `startupConfiguration.xml` | Component init order and verification steps |
| `storage/databases/schema.xml` | SQLite table definitions for all 7 databases |
| `Systems/radarManagement/rmConfig.xml` | Radar management configuration |
| `Systems/radarManagement/radar_messaging/radar_address_book.xml` | Radar RT/subaddress lookups |

---

## 14. Current Status Summary

### ✅ Fully Operational

- All five radar processors (Weather, Targeting, SAR, TFR, AEWC)
- Weather radar advanced capabilities: VIL, precipitation, storm cell tracking, wind shear detection, turbulence mapping
- MIL-STD-1553B bus simulation (BC + RT + all word types)
- Radar-to-Display bridge (direct sync path for all 5 radars)
- RadarDisplayDataCoordinator (TTL store + backup fallback)
- Cross-radar data fusion (`RadarDataFusion` singleton)
- Display class hierarchy (PFD, MFD, Holographic, EICAS, TSD, SMS)
- Enhanced rendering engine (Gaussian kernel + particle system)
- Display node state-tree system
- Visual / theme layer (Standard, Futuristic, Holographic themes)
- FMS integration: attitude, navigation, velocity, tactical data to PFD/MFD
- FCS integration
- Local messaging routing stack (unified router + handlers + response services)
- Predefined message library for all subsystems
- Automated 6-step installer
- Test suite (15+ test files)
- Defensive Systems (`Systems/defensiveSys/`) — RWR contact tracking with frequency-band
  threat classification, chaff/flare countermeasures with cooldown, ECM mode management,
  wired into `system_manager.py`, `eicas.py`, `tsd.py`. Verified via live run: generates
  RWR contacts, dispenses countermeasures, toggles ECM mode correctly.
- Sensor Management (`Systems/sensorManagement/`) — active/passive sensor managers,
  cross-sensor fusion with bearing-gate de-duplication, wired into `system_manager.py`.
  Had a severe bug (see Known Issues) that has been fixed and verified.
- Mission Planning (`Systems/missionPlanning/`) — targeting state machine, order-of-battle
  tracking, route management with waypoint/target designation, wired into
  `system_manager.py` and message routing. Had a dead-code bug (see Known Issues) that
  has been fixed and verified.
- Avionics Hardware Health (`Systems/avionics/hardwareHealth/`) — LRU status polling
  across 11 LRUs with health-state derivation, wired into `eicas.py`. The adjacent
  Built-In Test controller had a silent path bug (see Known Issues) that has been
  fixed and verified.

### 🐛 Known Issues

**August 20–21, 2026 — independent live-boot re-verification (external review session).**
A clean-clone review ran the full test suite (passing) and then attempted what no CI
test did: booting the actual application. It did not boot. Four distinct boot-path
defects were found and fixed, three of them instances of the SAME bug class — a
callback invoked while a non-reentrant `threading.Lock` is held re-acquires that lock:

| Issue | Location | Notes |
|-------|----------|-------|
| ~~Boot-blocking self-deadlock in `SystemStateManager.__new__`/`_initialize`~~ | ~~`Utils/common/system_state_manager.py`~~ | **Fixed (Aug 2026 re-verification, commit `6948a77`).** Introduced by Round 6's own TOCTOU fix (`8f37be6`): `__new__` held the class-level non-reentrant `_lock` while `_initialize()` re-acquired it. First construction deadlocked unconditionally → importing `core.system_manager` hung → **Main.py could not start at all from Aug 12 onward**, while CI stayed green (no CI test touched the boot path). `_initialize()` is now deliberately lock-free (only ever called under the lock, pre-publication). |
| ~~`VisualNode.update()`/`ModeNode.update()` self-deadlock on first display-tree update~~ | ~~`Interfaces/userInterface/displays/display_nodes/visual_node.py`, `mode_node.py`~~ | **Fixed (Aug 2026 re-verification, commit `e3419f5`).** Round 12 moved the notify outside the lock in the *base class* and made `_notify_subscribers()` acquire `self._lock` for its snapshot — but both subclass overrides still awaited the notify inside `with self._lock:`. `initialize_system()` hung forever at `initialize_radar_branch`. Notify moved outside the lock in both overrides, mirroring the base-class fix. |
| ~~`track_operation()` ran tracked callbacks while holding `_file_lock`~~ | ~~`Utils/common/operation_tracker.py`~~ | **Fixed (Aug 2026 re-verification, commit `e3419f5`).** Present since the initial commit; unmasked once boot got this far. The tracked `_initialize_database_manager` → `DatabaseManager.__init__` → `load_config` → `is_operation_completed()` chain re-acquired `_file_lock` → the qasync **event-loop thread froze on every boot** right after `SystemState.RUNNING` (marker only written after the callback, so it re-armed each run). Subsystem threads kept running — an operational-looking corpse. Callback now runs with the lock released; concurrent same-op callers wait on an in-progress `Event`. |
| ~~`systemsStartup.start()` never ran — two stacked un-awaited coroutines~~ | ~~`core/system_manager.py::start_FM_system()`, `core/systemsStartUp.py::_start_displays()`~~ | **Fixed (Aug 2026 re-verification, commit `5e09886`).** `self.systems_startup.start()` was called bare (async → coroutine created and dropped, RuntimeWarning each boot), so `_start_radar_system()`/`_start_displays()` had **never executed on any boot**; fixing that exposed the identical bug one level down (`display_management.start()` un-awaited). Now scheduled via the retained-task + logged-exception pattern / awaited directly. |
| ~~Documented entry point `python FMOFP/Main.py` did not work~~ | ~~`FMOFP/Main.py`~~ | **Fixed (Aug 2026 re-verification, commit `ff72980`).** Main.py had no path bootstrap (only SystemStart.py did), so the launch command printed by install.py raised `ModuleNotFoundError` immediately. SystemStart.py's bootstrap (sys.path, dual-path shim, CWD normalisation) mirrored into Main.py; verified from both `B20SS/` and the repo root. |
| ~~SQLite contention under the now-correct startup load~~ | ~~`storage/DBM.py` (pools for `flight_control_computer`, `default`)~~ | **Fixed (Aug 21 2026 follow-up session), live-verified.** First established the trigger: contention reproduces only on a **cold start** (no `FMOFP/tracking/` markers, no `storage/locks/`, no `.db` files) — warm runs show 0–1 unrelated errors, which is why it looked intermittent. Cold-start baseline: 2–16 `[DBM] Timeout waiting for available connection` ERRORs per run for `flight_control_computer`. Three real defects, in order of impact: **(1) `ConnectionPool.get_connection()` had its acquisition order backwards** — it blocked the *full* 5s timeout waiting for a returned connection *before* considering growing the pool toward `max_connections`, so with min=4/max=8 against a shared 20-worker executor the pool effectively grew one connection per five seconds of stall, then raised once saturated. Now: non-blocking take → grow if capacity remains → only then block. Measured directly: acquiring past `min` took **exactly 5.00s pre-fix, <0.01s post-fix**. **(2) Writers occupied pooled connections while blocked on the database lock** — SQLite permits one writer per file (WAL included), but every writer took a connection *first* and only then attempted `BEGIN IMMEDIATE`, so losers of that race sat in the busy handler holding pool capacity. Writers now queue on a per-database-path `RLock` (`get_db_write_lock()`, keyed by path because `default.db`/`radar_data.db` each back several systems) *before* acquiring a connection; this costs no throughput since SQLite already serialized them, and leaves the pool free for readers, who run concurrently under WAL. **(3) `connection_count` leaked on failed creation** — it was incremented *before* `create_connection()`, so a failed open permanently consumed a pool slot; also fixed a possible double-decrement in `return_connection()`'s error path and an unlocked increment in `initialize()`. Supporting changes: `busy_timeout` 5s→10s (aligned with the hand-rolled lock-retry deadlines; now only has to absorb *other processes*), `wal_autocheckpoint=256` to keep checkpoints short under the startup write burst, `default` pool max 5→10 and `flight_control_computer` 8→12. **Result: 0 pool timeouts across 5 consecutive cold starts** (baseline 2–16 each); boot to NORMAL unchanged at ~1.4s cold. Note for the record: WAL and `busy_timeout` were *already* enabled before this work — the original "needs WAL mode" framing was wrong; the bugs were in the pool algorithm, not the SQLite configuration. Covered by `Tests/test_db_connection_pool.py` (16 assertions), verified non-tautological by restoring the original acquisition order and confirming the timing assertion fails. |
| ~~Latent: databases directory not created on every init path~~ | ~~`storage/DBM.py::initialize_system_databases()`~~ | **Fixed (Aug 21 2026, found while reproducing the above).** `os.makedirs` for `storage/databases` ran only in the third branch of the init loop (the "not yet initialized" path). A process whose tracking markers or lock files already existed while the databases directory did not went straight to `SystemDatabase(...)` and failed with `unable to open database file` — live-reproduced. `makedirs` now runs once, unconditionally, before the loop. |
| ~~Graceful SIGTERM shutdown could not be verified in the review sandbox~~ | ~~`Main.py::signal_handler`~~ | **Closed — verified with externally-delivered SIGTERM (Aug 21 2026, follow-up session).** The earlier block was an environment artifact of the first review container (externally-delivered signals never reached Python handlers in any QApplication process there, reproduced with a 12-line PyQt-only script). In a different Linux container, a live 40 s offscreen `Main.py` run received an externally-delivered SIGTERM (from `timeout(1)`) and shut down gracefully: NORMAL → SHUTTING_DOWN → SHUTDOWN in ~7.3 s with orderly async-handler stop and a clean log tail. Windows behavior remains untested (no Windows environment available to any session so far). |

Boot verified end-to-end post-fix from a clean clone state: `SystemState`
INITIALIZED → STARTING → RUNNING → NORMAL in ~1s, systems-startup chain executing,
live radar→display data flowing, event loop responsive, zero un-awaited-coroutine
warnings. `Tests/ci_test_boot_smoke.py` (4 subprocess-isolated checks, including a
full boot-to-operational-state check) and `Tests/run_all_tests.py` (all 10
standalone-safe suites, one exit code) now run in both CIs, so "green CI,
unlaunchable app" cannot recur silently. Lesson repeated from the earlier audit
note, now with teeth: status claims here are only as good as what actually runs —
**the test suite passed for 8 days while the application could not start.**

| ~~70 undefined-name (F821) bugs, repo-wide~~ | 13 files: `mfd.py` (43), `RT_msg.py`/`flightManagementSystem.py`/`targeting_radar.py` (missing `traceback` import), `identifier.py` (missing `logger`), `MessageRoutingService.py`/`display_outgoing_router.py`/`AsyncMessageHandler.py`/`weather_radar_messages.py`/`node_based_pfd.py`, `baseStartUp.py` (1 left, deliberately) | **Fixed (August 2026, production-readiness re-analysis round).** `ruff check --select E9,F821,F7,F6` had never been run locally against this repo, and CI's own lint job piped its output through `\|\| true`, so it could never fail a build regardless of findings -- both gaps are now closed (CI select narrowed to F821 specifically, `\|\| true` removed). Highest-impact: `mfd.py` was missing `QFont`/`QPen` from its PyQt6.QtGui import, breaking `draw_systems_page()`/`draw_comms_page()`/`draw_engine_page()`/`draw_mission_page()` -- each wraps its body in a broad `except Exception as e: logger.error(...)`, so navigating the MFD to any of those four pages silently rendered a blank/broken page plus one ERROR log line instead of crashing outright. Live-verified fixed: all four now render with zero exceptions. Also fixed real bugs in `MessageRoutingService.route_event()` (never extracted `mode`/`display_type` from `event_data`, so display-mode-change routing had never once succeeded), `AsyncMessageHandler.py`'s `MessageBufferManager.add_message()` (never extracted `request_id`, so the priority-buffering path had never worked), and three files missing `import traceback` inside their own exception handlers (a bug that only manifests when the exception it's meant to report already fired, masking the original error). One finding left deliberately unfixed and documented in place: `core/baseStartUp.py`'s `SchemaManager` reference has no implementation anywhere in the codebase, but the whole containing class is confirmed dead code (nothing imports `core.baseStartUp`), so fabricating an implementation would be a guess, not a fix. See commits `bf0ca63`, `175520f`. |
| ~~Stale "TODO LATER" comment + inconsistent `send_completion` handling in `radarControl.py`~~ | ~~`Systems/radarManagement/radarControl.py::_configure_enroute_surveillance()`~~ | **Fixed (August 2026).** Claimed "other radar types don't support this parameter YET", but `targeting_radar.py`/`tfr_radar.py`/`sar_radar.py`/`aewc_radar.py` all already define `set_mode(self, mode, send_completion=True, request_id=None)` -- the comment was stale. Also inconsistent with the near-identical `_configure_airport_arrival()` method right below, which already passes `send_completion=False` "for all startup mode changes to prevent unnecessary messages" for every radar type. Now passes `send_completion=False` consistently across all five radar types in both methods. |

| Issue | Location | Notes |
|-------|----------|-------|
| ~~Unguarded check-then-create race in every subsystem singleton's start()~~ | ~~19 files: PowerManagementSystem, FuelManagementSystem, ThrustManagementSystem, HydraulicSystemController, AirframeSystemManager, ECSControl, FlightDataMonitoring, FlightManagementFitness, DefensiveService, FlightSurfaceService, GCAS, PerformanceMonitor, SensorService, CommsService, MissionService, NavService, EngineControlUnit, FlightManagementSystem, FlightControlComputer~~ | **Fixed (August 2026).** `system_manager.py`'s `start_FM_system()` calls `x.start()` directly on every Phase 2/3 component, and separately, `start_async_components()`'s generic "start remaining components" pass *also* calls `component.start()` for anything in `self.components` with a `start` attribute -- both paths can call a given singleton's `start()` close enough together to race an unguarded `if self._thread is None or not self._thread.is_alive(): create-and-start` pattern, spinning up two independent background threads for the same instance. Found while extending a routine boot test from ~15s to 25s+ (long enough for both invocation paths to actually overlap) to exercise the 8 subsystems wired in earlier this round; proven concretely on `FlightDataMonitoring`, whose `collect_data()` log volume ran at ~2x the single-thread expectation and dropped back to the expected rate once locked. Re-reading every other singleton's `start()` afterward found the identical unguarded pattern in all 9 pre-existing Phase 2/3 subsystems plus `FlightManagementSystem` and `FlightControlComputer` -- this was a systemic gap in the codebase's singleton-service convention, not something introduced by this session's own wiring work; it just hadn't been exercised long enough to surface until now. Fixed uniformly by wrapping each `start()`'s check-then-create sequence in a lock (reusing each class's existing data lock where one exists -- confirmed none of them touch that lock from within `start()` itself, so no reentrancy risk -- or a new dedicated lock where none existed, e.g. `NavService`). `FlightManagementSystem` was the most consequential instance: an unguarded double-call there would start a second `flight_control_system`, a second GPS thread, and a second FMS thread all at once, since FMS is what nearly every other subsystem's lazy-init reaches via `get_flightManagementSystem()`. Verified live: after the fix, "FMS is already running" was observed logging twice in one boot (proving `start()` really was called 3 times total, twice correctly rejected instead of racing) with zero duplicate threads. Also checked and found already safe: `MessageQueueManager` (already guards its check-then-create with `self.__class__._lock`). Also checked and found not currently exploitable: `ScenarioEngine` (its `start()` bails out on "no events loaded" before ever reaching thread creation, since nothing loads a scenario at boot); `Systems/comms/dataLink/data_Link.py`, `satcom/satCom.py`, `radios/radio.py`, and `Systems/avionics/hardwareHealth/LRUstatus.py` (same unguarded pattern, but dead code -- nothing anywhere imports or calls `.start()` on any of them). **Update (August 2026, production-readiness follow-up):** `Utils/common/async_thread_wrapper.py`'s `AsyncThreadWrapper` class was flagged during the original sweep but not yet audited -- confirmed via grep across the whole tree that nothing imports it (the only other `AsyncThreadWrapper` in the codebase, in `interface_display_message_handler.py`, is an unrelated, same-named class with no persistent thread state to race), so it poses no live risk today, same as the other dead-code files in this list. Noted for whenever it is eventually wired in: its own `start()` has a separate, still-live latent bug -- it creates `self.thread` but then hands a second, different `Thread` object to `thread_manager.add_thread()`/`start_thread()`, so `self.thread` itself never actually runs and `self.thread.is_alive()` would never be `True`, meaning its own is-already-running guard could never trigger. Out of scope to fix now since nothing calls it. Full CI-equivalent suite and repeated full boot+SIGTERM cycles (both short ~15s and extended ~25s) all pass clean throughout. See commits `0ec68ba`, `b7d3779`, `0c33928`, `b2a4761`. |
| ~~"X started" INFO line logged twice for many subsystems~~ | ~~same 19 files as above, plus `core/system_manager.py`~~ | **Investigated and resolved -- not a bug.** Initially looked like a symptom of the race above, but a logging-internals diagnostic (callHandlers/Handler.handle/emit/stream.write all traced, confirmed exactly one real dispatch per real log call) proved otherwise. The actual explanation, found by grepping the source tree for the literal duplicated string: there are two separate, intentional `logger.info()` call sites for every Phase 2/3 component -- one inside the subsystem's own `start()` (confirms its thread came up) and one in `system_manager.py`'s Phase 3 block, logged right after `x.start()` returns (confirms the boot step completed). This two-tier pattern is pre-existing and universal (`EngineControlUnit`, `NavService`, etc. already did this); it just wasn't visually obvious for those because their internal message carries a distinguishing `[TAG]` prefix and different capitalization from `system_manager.py`'s line. The 8 subsystems wired in this round used the bare subsystem name with no prefix, making their internal line byte-identical to `system_manager.py`'s line instead of merely similar. Added a `[TAG]` prefix to each (`[PMS]`, `[FUEL_MGMT]`, `[HYDRAULICS]`, `[AIRFRAME]`, `[ECS]`, `[FDM]`, `[FMFITNESS]`) matching the existing convention, purely for log readability -- the double log line itself is expected and harmless. See commit `b7d3779`. |
| ~~Non-weather radar message classes could not be constructed at all~~ | ~~`local_messaging/messageConfigurations/base_message.py`, `RadarMessageHandler.py`, `aewc_radar_data.py`, `targeting_radar_data.py`, and 18 other message-class files~~ | **Fixed (August 2026).** Live-exercising all 10 radar-type-specific data handlers in `RadarMessageHandler.py` (TFR elevation/terrain-warning, SAR imagery/stripmap/spotlight/scansar, targeting track, AEWC sector/stealth) — the previous audit pass had only read the code, not run it — found every single one hit a real error internally, silently caught and logged by each handler's own broad `except Exception`, so a naive "did it raise" check reported false PASS for all 10. Root cause: `BaseMessage` is a strict `@dataclass` with only five declared `*_uuid` fields, but ~20 message-class files across TFR/SAR/targeting/AEWC/FCS pass their own domain-specific identifier kwargs (`data_uuid`, `warning_uuid`, `config_uuid`, etc. — ~50 call sites) straight through to `super().__init__()`, which had no `**kwargs` catch-all — `TypeError` on every construction. Fixed by giving `BaseMessage` a hand-written `__init__` (behaviorally identical to the generated one, verified via `dataclasses.fields()`/`asdict()`/`is_dataclass()`) that stores unrecognized kwargs as plain attributes instead of raising; also fixed a second bug in the same class found while rewriting it (`timestamp` was frozen at process-start instead of generated per-message). Three further, independent bugs found via the same live exercise: (a) 8 message classes in `aewc_radar_data.py`/`targeting_radar_data.py` called `super().__init__()` positionally with the wrong argument order, silently writing `sending_system`'s value into the `timestamp` field and `destination`'s value into `message_uuid` — no exception, `sending_system`/`destination` were just always `None`; (b) `RadarMessageHandler.py`'s wildcard import only pulls in the 4 weather message classes `messageConfigurations/__init__.py` actually re-exports, so 6 classes used by name elsewhere in the file (`tfr_radarElevationProfile`, `tfr_radarTerrainWarning`, `sar_radarImagery`, `sar_radarStripMap`, `sar_radarSpotlight`, `sar_radarScanSAR`) raised `NameError` on every call; (c) 3 classes referenced by name (`targeting_radarTrackData`, `aewc_radarSectorData`, `aewc_radarStealthData`) did not exist anywhere in the codebase — created all three, matching the exact keyword arguments already used at their call sites. A fifth bug, also only visible once the above were fixed and the messages actually reached validation: all 10 of these "_update" notification messages (plus `weather_shear_update`/`weather_turbulence_update`, which were separately failing the same gate) were built without a `command_word`, which `AsyncMessageHandler._validate_message()` hard-requires for any non-status message — every one was silently dropped at validation. Fixed by including the already-registered command word for each message's radar/data type. Verified live: all 10 handlers now run with zero ERROR-level log output (down from 8 `NameError`s + 2 validation failures); full CI-equivalent suite (27/27, 33/33, weather radar, scenario engine) and a full boot+SIGTERM cycle both pass with no regressions. See commit `36c85e7`. |
| ~~Weather radar → display via 1553B chain~~ | ~~`precipitation_response_service.py`, `vil_response_service.py`~~ | **Fixed** — removed re-entry call to `DisplayMessageHandler` from both response services; bridge push is now the sole delivery path. |
| ~~Debug logging at ERROR level~~ | ~~`vil_data_handler.py`, `precipitation_data_handler.py`~~ | **Fixed** — all 26 `logger.error("[*_DEBUG]")` calls downgraded to `logger.debug()`. |
| ~~CLI status request not implemented~~ | ~~`Utils/debug/userCLI.py:142`~~ | **Fixed** — `_handle_status_command()` implemented covering system state, all 5 radars, FMS, FCS, Nav, Comms, and 1553B bus. |
| ~~CLI input thread crashes + fails to restart when run headless~~ | ~~`Utils/debug/userCLI.py::get_commands()`~~ | **Fixed (August 2026).** Found running a full boot in a non-interactive environment (no terminal attached to stdin): `input()` raised `EOFError` on the first call, which the loop's broad `except Exception` caught, logged as an ERROR, and let the whole thread exit after one iteration. The startup sequence then tried to restart the same (already-finished) `Thread` object, which Python always rejects — "threads can only be started once" — producing 3 more ERROR-level lines. Not a simulation-logic bug (a real interactive session has a real stdin, so `input()` just blocks normally), but a real robustness gap for headless/CI/automated launches. Fixed by catching `EOFError` specifically around the `input()` call, logging it once at INFO level, and having the loop skip further input attempts via a dedicated flag instead of letting the thread exit — it now stays alive in an idle state for the rest of the run. Verified live: reproduced the original 4-error boot, applied the fix, re-ran — zero errors, thread reports `Alive: True` for the remainder of a 38s sustained run. |
| ~~scipy missing from requirements.txt / install.py~~ | ~~`requirements.txt`, `install.py`~~ | **Fixed (August 2026).** `stormCellTracking.py`, `precipitation_analysis.py`, and `weather_processor.py` all do a bare `from scipy import ndimage` (a fourth file, `sar_processor.py`, already guards it correctly with try/except), but `scipy` was never listed in `requirements.txt` and nothing else in the dependency chain pulls it in transitively. Confirmed live this silently broke precipitation analysis and storm-cell segmentation on a clean install: ~40 "Error analyzing precipitation: No module named 'scipy'" errors per 15s of steady-state weather-radar operation, caught by a broad except so the app kept running but the feature never worked. Also found `install.py`'s actual installer step (`install_dependencies()`) iterates over a separate hardcoded `REQUIRED_PACKAGES` list, not `requirements.txt` directly, so adding scipy to `requirements.txt` alone would not have been sufficient -- fixed both. Verified live: `install._is_installed('scipy')` now reports `True` and a full boot produces zero scipy-related errors. |
| ~~"No request ID provided" ERROR-level traceback on every internal/automatic radar mode transition~~ | ~~`Systems/radarManagement/radar_messaging/completion_message_handler.py`~~ | **Fixed (August 2026).** `send_completion_message()` raised a bare `ValueError` whenever `request_id` was `None`, which every internal/automatic mode transition (initial radar configuration at boot, mission-phase-driven resource management) legitimately does by design -- there's no external 1553B request to acknowledge when nothing asked for the change. The exception was caught by a broad `except` one level up and logged as a full ERROR-level traceback each time, which is log noise for an expected condition, not a real failure (confirmed live: 3 occurrences in every 15s boot, one per radar's initial mode set, not something that continuously spams). Changed to log at DEBUG and return `False` directly -- functionally identical to before (callers already handle a `False` return gracefully) but without the spurious stack trace. |
| ~~Graceful shutdown (SIGTERM/SIGINT) hangs indefinitely, requiring SIGKILL~~ | ~~`core/system_manager.py`, `Systems/radarManagement/radarControl.py`, `MIL_STD_1553B/Messaging.py`, `Utils/common/thread_manager.py`~~ | **Fixed (August 2026), with one honestly-documented residual gap.** Live-reproduced: sending SIGTERM to a running `SystemStart.py` never let the process exit; it always required `SIGKILL`. Root-caused to five separate, previously-unknown bugs, all found by reproducing live and reading exactly where the shutdown log stalled each time: (1) `system_manager.stop_system()` called `thread_manager.stop_thread("RadarMain"/"RadarManagement")` — a blocking, timeout-less `thread.join()` — *before* anything told those threads to stop, and separately called `radar_management.stop()` bare even though it's `async def`, so the call just constructed-and-discarded a coroutine without ever running it; (2) `RadarManagementSystem.update()`'s per-iteration `with self._lock:` wrapped calls to `_manage_radar_resources()`, which *itself* independently does `with self._lock:` on the same non-reentrant `threading.Lock` — an immediate, permanent self-deadlock on the very first call, confirmed live via its debug log firing exactly once at startup and never again for the process's entire lifetime, meaning this thread had never actually done any resource/config/mode-transition work, ever; (3) `RadarManagementSystem.initialize()` starts a `RadarControlEventLoop` thread running `self._loop.run_forever()`, and nothing anywhere ever called `self._loop.stop()`, so that thread could never exit under any circumstance; (4) `ScheduleMessage.periodic_load_message_rates()` used a single `time.sleep(300)` with no way to interrupt it, so `stop()`'s `thread.join()` could hang up to 5 minutes; (5) `Schedule.run_major_frame()` had the identical pattern with `time.sleep(remaining_time)` (~1000s with default config). Fixed each at its root (reordered the radar stop sequence and properly scheduled the coroutine via `loop.create_task()`; removed the redundant nested lock acquisition; explicitly stop the radar event loop via `call_soon_threadsafe(loop.stop)` before joining; replaced both long sleeps with 1s-granularity interruptible loops of the same total duration). Also added two systemic, defense-in-depth changes so no *other* thread (present or future) can repeat this class of bug: `ThreadManager.stop_thread()` now joins with a bounded 2s timeout instead of joining forever, logging an error and moving on if a thread doesn't exit in time; and `ThreadManager.add_thread()` now creates all threads as `daemon=True`, so the process can still exit even if some thread genuinely can never be joined. **Update (August 2026, second pass):** the residual gap described above is now fixed. `BC_Listener`/`RT_Listener` already used non-blocking `select.select(..., timeout)` polling internally, but nothing ever called `Bus_Controller.stop_listener()`/`Remote_Terminal.stop_listener()` in the first place, and `stop_listener()` itself didn't hold a reference to the actual listener instance to stop it — fixed in `BC.py`/`RT.py`/`system_manager.py`. The four `UserCLI_*` threads plus a fifth, separate `user_cli` thread (a genuinely infinite `while True: time.sleep(10)` loop with a dead `except KeyboardInterrupt` handler, since that exception is never delivered to background threads) were blocked because `UserCLI.stop_cli_threads()` — the correct, pre-existing mechanism — was never called by anything; also switched `input()`/unbounded `.wait()` to `select.select()`-based polling / bounded timeouts (POSIX only) in `userCLI.py` so the threads can actually notice the flag. A sixth thread pair (`DisplayMessageProcessor`/`DisplayMessageCleanup`) was found during this pass: two different classes (`InterfaceDisplayMessageHandler` and `DisplayMessageHandler`) race to register the same thread names, the interface handler wins and actually owns the running threads, but shutdown only ever called `.stop()` on the other one — `InterfaceDisplayMessageHandler` had no `stop()` method at all; added one. Result: `thread_manager` now reports zero "did not stop within timeout" errors (down from 7-8+ per run) and `stop_system()` completes in ~4s. Chasing this also surfaced and fixed a deeper bug: `Initializer._initiate_shutdown()` called `self.cleanup()` synchronously while still nested inside the executing shutdown asyncio task, which is illegal reentrant `run_until_complete()` usage and threw `RuntimeError: Cannot enter into task ... while another task ... is being executed` on every shutdown; fixed by only scheduling `loop.stop()` there and letting `SystemStart.py`'s own `finally: cleanup()` run it safely once `run_forever()` actually returns. Even so, live testing showed `run_forever()` (which wraps `QApplication.exec()`) can fail to return on its own because free-running Qt `QTimer`s (e.g. the ~60fps weather radar animation) keep Qt's native loop pumping independent of asyncio's stop flag — by that point every thread/resource is already verified stopped, so a bounded 5-second daemon watchdog (`threading.Timer` → `os._exit(0)`) was added as a safety net, matching how orchestrators (systemd/k8s/docker) already treat graceful shutdown. Two more real bugs were found and fixed along the way, both firing on every shutdown: `radarControl.py` was `await`-ing `RadarMessenger.stop()`, which is a synchronous method, raising `TypeError: object NoneType can't be used in 'await' expression`; and `EnhancedRadarResponseService` had no `stop()` method at all despite `MessageRoutingService.stop()` unconditionally awaiting it, raising `AttributeError` every time. Process now provably exits within ~9s of SIGTERM (previously indefinite / required SIGKILL). Verified via repeated live SIGTERM tests plus a full CI-equivalent suite re-run (27/27, 33/33, weather radar, scenario engine all pass). One additional side effect surfaced by fix (2): with `_manage_radar_resources()`/`_check_radar_configurations()`/`_handle_mode_transitions()` finally able to run for the first time, `_get_expected_mode()`'s unimplemented fallback (`return RadarMode.STANDBY`, explicitly marked `# Add similar logic for other radar types`) started firing ~70 "Invalid mode type" errors in an 8s window for targeting/AEWC radar — this fallback was dead code before and had never actually executed. Fixed by returning the radar's own current mode as a neutral "no policy defined yet" default instead of a wrong-typed enum, which cleanly stops the spam without inventing unspecified mission-phase behavior. Verified live throughout: each fix individually reproduced-then-resolved via targeted SIGTERM tests with `py-spy`/timestamp-correlated log analysis; full boot re-run clean (zero "Invalid mode type", zero new tracebacks beyond one pre-existing unrelated `completion_message_handler.py` issue); full CI-equivalent suite (27/27, 33/33, weather radar, scenario engine) passes. |
| ~~LIDAR scan() generated up to 1,000,000 detections per call~~ | ~~`sensorManagement/activeSensors/activeSensors.py`~~ | **Fixed** — `Lidar.scan()` looped `range(self.pulse_rate)` (1,000,000), and `SensorDataFusion._merge_or_add()`'s O(n) per-contact gate check made this O(n²), hanging the sensor poll thread indefinitely the first time LIDAR was activated. Bounded to `random.randint(0, 5)`, matching `Radar.scan()`'s simulation density. |
| ~~sensorService.py / sensorDataFusion.py monkey-patch workaround for a non-existent bug~~ | ~~`sensorManagement/sensorService.py`, `sensorManagement/sensorDataFusion.py`~~ | **Fixed** — both bypassed `ActiveSensorManager`/`PassiveSensorManager.__init__` via `object.__new__()` and hand-duplicated their sensor lists, citing a `self.logger = logger("name")` bug that does not exist anywhere in either file's current code or git history. Replaced with normal constructor calls. |
| ~~`MissionService.add_waypoint()` / `add_target()` each defined twice~~ | ~~`missionPlanning/missionService.py`~~ | **Fixed** — the earlier `add_waypoint(lat, lon, alt_ft, name='')` was dead code shadowed by a later, incompatible `add_waypoint(name='', lat=0.0, lon=0.0, alt_ft=0.0, wp_type='NAV')`. No current caller was affected (all use keyword args), but any future positional caller would have silently misassigned arguments. Dead definitions removed. |
| ~~`BuiltInTestController` config path missing `FMOFP/` segment~~ | ~~`Systems/builtInTestSystems/bitControl.py`~~ | **Fixed** — `ET.parse("Systems/builtInTestSystems/bitsConfig.xml")` doesn't exist under any working directory (`B20SS/Systems/` isn't a real path; only `B20SS/FMOFP/Systems/` is), so construction always raised `FileNotFoundError`. `eicas.py` swallows this in a broad `except Exception: logger.debug(...)`, so the EICAS BITS panel silently never populated. Fixed by resolving the config path relative to the file's own location. **Note:** `Systems/fuelSystems/fuelControl.py` has the identical bug pattern -- already confirmed fixed, see the Stub/Incomplete Subsystems table below (this note predates that verification and was left stale). |
| Hardcoded MIL-STD-1553B bus ports/IPs (not a bug, documented finding) | `MIL_STD_1553B/Remote_Terminal/RT_connect/RT_socket.py`, `MIL_STD_1553B/Bus_Controller/BC_connect/BC_socket.py` | **Investigated (August 2026 re-analysis round), left as-is by design.** `RT_sender`/`BC_sender` hardcode `destination_ip = "localhost"` plus ports 5000 (BC_Listener) / 5001 (RT_Listener); confirmed via grep there is no `os.environ`/`getenv`/`ConfigParser` override anywhere in either file. Also swept the rest of the repo for other portability red flags (developer-machine absolute paths like `C:\\...`, `/home/...`, `/Users/...`, and non-localhost hardcoded IPs) and found none -- this pair of files is the only place with fixed networking values. Concluded this is an acceptable simplification, not a fix-worthy bug: BC and RT are both simulated in-process on the same machine (this is a single-host avionics simulator, not a distributed service), so `localhost` is actually correct for the intended deployment model, and the fixed ports only risk a collision (`OSError: Address already in use`) if two instances of the simulator are run simultaneously on the same machine -- a scenario this tool was never designed for. Adding env-var/config-file port overrides would be a genuine feature addition (multi-instance support), not a bug fix, so it's left for the Next Steps / Long-term backlog below rather than implemented speculatively. |
| `run_tests()` result discarded / never checked -- false-positive risk in several test entry points | `Tests/predefined_messages_test.py`, `Tests/combined_precipitation_vil_flow_test.py`, `Utils/debug/userCLI.py` | **Fixed (August 2026 re-analysis round, test-assertion-quality spot-check).** Two separate but related bugs found while spot-checking pre-existing (not this-session-authored) tests for false-positive risk, per the same methodology used for the automated CI suite: (1) `predefined_messages_test.py`'s `run_tests()` recorded individual message-check pass/fail via `_record_test_result()` into `self.results["summary"]`, but its final `return` was an unconditional `return True` that never consulted that summary -- so it could only ever return `False` if a raw, unhandled exception escaped every one of its per-message try/except blocks, which none of them do (each catches its own errors and records the failure instead of raising). (2) `combined_precipitation_vil_flow_test.py`'s `test_combined_data_flow()` had no explicit return on its success path (implicitly returned `None` despite its own `Tuple[bool, bool]` type hint), and its caller `run_tests()` discarded that return value entirely rather than propagating it. Fixing this also surfaced a second, smaller pre-existing issue in the same method worth calling out: the VIL half of this "combined precipitation + VIL" test has its actual VIL-send call commented out (confirmed via `git log --follow` to be disabled since the initial commit, not a regression), so only the precipitation half is genuinely exercised -- the fix returns `None` (not `True`/`False`) for the un-exercised VIL half so it can't silently count as either a pass or a fail, and `run_tests()`'s overall result now derives only from the part actually tested. Both bugs meant `Utils/debug/userCLI.py`'s CLI `test` menu -- which branches on these methods' return values to print "Test completed successfully!" vs "failed!" -- would report success regardless of how many individual checks actually failed. Also found the same discard-the-result pattern (independent of the two bugs above) in three of `userCLI.py`'s own CLI wrapper methods (`combined_precipitation_vil_flow_test()`, `fms_system_test()`, `flight_control_system_test()`), which called `await test_suite.run_tests()` without capturing the return value at all, even though `fms_system_test.py`/`flight_control_system_test.py` already computed a correct `overall_success` boolean internally -- it just was never read. All fixed to derive/propagate a real pass/fail signal, matching the pattern already used correctly by the CLI's `predefined_messages_test()` wrapper and by `weather_radar_surveillance_mode_test.py`/`fms_system_test.py`/`flight_control_system_test.py`'s own internal `all([...])`-based result computation (those three were spot-checked and found NOT to have this bug -- only the two files above did). Verified the fix logic in isolation against simulated summary dicts (all-pass, partial-failure, all-fail cases); full live re-run of `predefined_messages_test.py`/`combined_precipitation_vil_flow_test.py` was not performed since both require a fully running message-bus/system-manager context reached only via the interactive debug CLI, but the fix is a small, mechanically-verified boolean derivation from data these files already collect correctly, with zero behavioral change to the actual test logic itself. |
| 15 of 25 debug-CLI `test` menu entries reference test module files that don't exist in the repo | `Utils/debug/userCLI.py` (e.g. `weather_radar_display_visual_test`, `vil_display_flow_test`, `precipitation_display_flow_test`, `echo_top_display_flow_test`, `weather_radar_display_test`, `weather_radar_end_to_end_test`, `precipitation_system_test`, `vil_system_test`, `sar_radar_mode_change_system_test`, `targeting_radar_mode_change_system_test`, `aewc_radar_mode_change_system_test`, `tfr_radar_mode_change_system_test`, `weather_radar_node_test`, `weather_radar_visual_test`, `display_individual_mode_test`, `weather_radar_mode_change_system_test`) | **Found (August 2026 re-analysis round), documented rather than fixed.** Cross-referenced every `importlib.import_module('FMOFP.Tests....')` call in `userCLI.py`'s test-runner methods against the actual contents of `FMOFP/Tests/` (including the `radar_tests/` subpackage) -- 15 of 25 referenced modules do not exist anywhere in the repo or its git history (confirmed via `git log --follow`), so selecting any of these entries from the debug CLI's interactive test menu raises an unhandled `ModuleNotFoundError` (live-verified for one: `weather_radar_display_visual_test`). This is a different failure mode than the false-positive bugs above -- it fails loud rather than silently, so it can't mask a real bug the way the `run_tests()` issues did -- but it means most of the CLI's test menu is currently non-functional. Not fixed this round: writing 15 new integration test files (each of the working equivalents like `flight_control_system_test.py`/`fms_system_test.py` runs 1000+ lines with live message-bus tracing) would be fabricating substantial new test content rather than a verification-driven fix, and pruning the stale menu entries risks changing CLI behavior without knowing why they were never implemented. Left as a scoped backlog item; see Next Steps below. |
| Uninterruptible `time.sleep(5)` error-backoff in 12 background-thread `run()`/`update()` loops -- `stop()` could return while the thread was still alive | `Systems/flightSurfaces/flightSurfaceService.py`, `Systems/engineManagement/ecu/engineControlUnit.py`, `Systems/engineManagement/thrustManagement/thrustManagement.py`, `Systems/defensiveSys/defensiveService.py`, `Systems/flightDataMonitoring/fdmControl.py`, `Systems/missionPlanning/missionService.py`, `Systems/powerManagement/elec/powerManagementSystem.py`, `Systems/comms/messaging_service.py`, `Systems/comms/dataLink/data_Link.py`, `Systems/comms/satcom/satCom.py`, `Systems/comms/radios/radio.py`, `Systems/flightControlSys/flightControlComputer/flightControlComputer.py` | **Fixed (August 2026, re-analysis round, live-verified).** All 12 of these subsystems' background loops follow the same pattern: `while not self._running.is_set(): try: ...work...; except Exception: time.sleep(5)` for error-backoff, with `stop()` calling `.set()` then `thread.join(timeout=2)`. Plain `time.sleep()` is not interruptible by the Event being set, so if `stop()` is called while the thread is in that 5s backoff, `join(timeout=2)` times out and returns -- `stop()` logs "... stopped" and control returns to the caller while the background thread is, in fact, still alive and will keep running for up to ~3 more seconds. Live-reproduced on `ThrustManagementSystem` before fixing: forced an exception into the update loop, called `stop()`, measured it return after exactly 2.00s with the thread still alive (`thread.is_alive()` -> `True`), which then self-terminated ~3s later, outside the join window. Fixed by replacing the bare `time.sleep(5)` with the Event's own `.wait(5)`, which is interruptible by `.set()`; re-verified live on both `ThrustManagementSystem` (`self._running`) and `PowerManagementSystem` (`self.running`, the other of the two attribute-naming conventions in use) -- `stop()` now returns in ~0.000s with the thread confirmed dead immediately, in both the forced-exception case and normal operation. This was found via a broader sweep (resource-leak / stop()-shutdown-path check) prompted by the start() TOCTOU race fixed earlier this session; separately concluded that `stop()` itself has no unguarded concurrent-call race worth fixing, since system_manager.py's boot (`start()`) and SIGTERM-triggered shutdown (`stop()`) sequences are both strictly sequential on the main thread in this codebase's actual usage -- unlike `start()`, which genuinely was called concurrently by design (a direct Phase 3 call plus a generic "start remaining components" pass). |
| XML config loading has no per-class defensive try/except (investigated, not a bug) | `HydraulicSystemController`, `AirframeSystemManager`, `FlightManagementFitness`, `FlightDataMonitoring`, `SoftwareConfigManager`, and other config-driven subsystem `__init__` methods | **Investigated (August 2026 re-analysis round), confirmed already safe by design.** None of these classes' `ET.parse(_CONFIG_PATH)` calls are wrapped in try/except at the point of use -- a missing or malformed config file raises `FileNotFoundError`/`xml.etree.ElementTree.ParseError` straight out of `__init__`. However, `core/system_manager.py` already wraps every one of these components' construction+start in its own `try/except Exception as e: logger.error(f"... startup failed (non-fatal): {e}")` at the call site, so a bad config for any one subsystem is isolated and non-fatal -- the rest of the boot sequence continues normally. Live-verified: temporarily replaced `hydrConfig.xml` with invalid XML and ran a full boot -- produced exactly one `HydraulicSystemController startup failed (non-fatal): syntax error...` line, all other subsystems (confirmed: Airframe System Manager, Environmental Control System) started normally immediately after, and the process still shut down cleanly on SIGTERM. Config file restored and diffed against a backup to confirm no residual corruption. No code change needed. |
| `pip-audit` dependency vulnerability scan (new this round, clean) | `requirements.txt` | **Checked (August 2026 re-analysis round).** Ran `pip-audit -r requirements.txt` against the full pinned/bounded dependency set (`PyQt6==6.8.1`, `PyQt6-Qt6==6.8.2`, `PyQt6-sip==13.10.0`, `numpy>=1.26,<3.0`, `qasync>=0.27`, `click>=8.0,<9.0`, `scipy>=1.11,<2.0`) -- zero known vulnerabilities found. No action needed; noting the clean result for the record since this is the first time this check has been run this session. |
| Unbounded log file growth -- plain `FileHandler` with no size cap or rotation | `Utils/logger/sys_logger.py` | **Fixed (August 2026 re-analysis round).** `setup_logging()` used a plain `logging.FileHandler`, at the default DEBUG level (`startupConfiguration.xml`), for the entire lifetime of the process -- no size cap, no rotation. Measured live: a single ~25s boot+shutdown test alone produced a 508KB / 4767-line log file, which extrapolates to hundreds of MB over a multi-hour training session with nothing to bound it -- a real disk-filling risk for a simulator meant to run for extended periods (`cleanup_old_logs()` only wipes files *between* runs, it doesn't cap growth *within* one). Switched to `logging.handlers.RotatingFileHandler` (50MB per file, 5 backups = 250MB worst case per session) -- identical behavior for short/typical runs, bounded worst case for long-running ones. Verified live: confirmed the root logger's file handler is now a `RotatingFileHandler` with the expected `maxBytes`/`backupCount`, and separately verified the rotation mechanism itself (forced rotation with a small `maxBytes` in an isolated test) correctly rolls old data into capped `.1`/`.2`/`.3` backup files rather than growing one file forever. |
| `DatabaseManager.shutdown()` existed but was never called anywhere | `storage/DBM.py`, `core/system_manager.py` | **Fixed (August 2026 re-analysis round).** `DatabaseManager.shutdown()` is a complete, correct implementation -- closes every system's pooled SQLite connections and shuts down its internal `ThreadPoolExecutor` -- but a repo-wide search found zero callers anywhere in the codebase; every connection pool (2-8 connections x 17 configured systems) and the worker pool were simply abandoned at process exit rather than closed. Not something that has caused any observed hang in this session's many live SIGTERM tests (the OS reclaims file descriptors on process exit regardless), but a genuine, low-risk, trivial-to-fix cleanup gap. Added a call to `DatabaseManager('FMOFP/dbConfig.xml').shutdown()` (the class is a true `__new__`-based singleton, so this reaches the same shared instance every other subsystem already uses) at the end of `system_manager.py`'s `stop_system()`, after every subsystem's own `stop()` has already run so nothing is still writing when connections close. Verified live: full boot+SIGTERM test shows `"Shutting down DatabaseManager"` -> `"[DBM] DatabaseManager shutdown complete"` in the log with zero new errors, and the process still exits cleanly. |
| Related but lower-priority: DBM's own rate-limiter blocking sleep is also uninterruptible (documented, not fixed) | `storage/DBM.py::rate_limit_query()` | **Investigated (August 2026 re-analysis round), documented rather than fixed.** `rate_limit_query()` calls a plain, uninterruptible `time.sleep(sleep_time)` when a system exceeds its configured query-rate limit -- `sleep_time` can be as long as that system's configured `duration` + 1 second, up to 120s for some radar systems per `dbConfig.xml`. This is the same bug class as the 12-file `time.sleep(5)` fix above, but centralized in the shared `DatabaseManager`/`SystemDatabase` layer used by every subsystem's DB calls, and the sleep is much longer in the worst case. Unlike the 12 subsystem loops (each of which owns its own `threading.Event` that `stop()` can set), `rate_limit_query()` has no natural per-caller stop signal to check -- fixing it properly would mean threading a cancellation/stop signal through the shared DB layer used by all ~17 configured systems, a broader architectural change than a targeted verification-driven fix. Assessed the real-world severity as low rather than forcing a risky fix: every subsystem's configured rate limit is well above its actual steady-state write rate (e.g. `power_management_system` allows 60 inserts/30s against an actual ~1/s insert loop), and grepping every boot-test log produced this session (5 full boot+SIGTERM runs across this and the prior round) for "Rate limit exceeded" returned zero matches -- this path has not fired once in any live test performed. It was previously observed to fire only inside a test harness that deliberately hammered `FuelManagementSystem` construction in a tight loop (documented in the Test Coverage section), not during normal operation. Left as a known, scoped, low-priority architectural gap rather than an active bug. |
| Unlocked read of a lock-protected list length (minor consistency fix, not a crash risk) | `Systems/flightControlSys/performaneMonitoring/performaneMonitoring.py::get_status()` | **Fixed (August 2026 re-analysis round).** Broader sweep of ~16 subsystem singletons' `get_status()` methods for unlocked reads of state mutated by their own background update thread found this class already almost universally well-guarded (most either don't mutate shared collections after `__init__`, e.g. `FlightDataMonitoring.storage`/`.recorders`, or already route every read through a locked accessor, e.g. `PowerManagementSystem.get_status()`'s `with self.lock:`). One real inconsistency found: `PerformanceMonitor.get_status()` read `len(self._history)` directly, while its two sibling values in the same dict (`get_exceedances()`, `get_avg_fuel_efficiency()`) both correctly acquire `self._lock` -- `_record_history()` mutates `self._history` under that same lock every ~1s from the update loop. `len()` on a Python list is GIL-atomic, so this was never a crash risk, just a momentary staleness/consistency gap versus its neighbors; locked for consistency. Live-verified `get_status()` still returns correctly (no deadlock from the added lock acquisition) via a direct start/get_status/stop smoke test. |
| `DisplayMessageHandler.send_request()`'s `pending_requests` dict grew unbounded -- no response-handling path ever read or removed a successful entry | `local_messaging/routing/handlers/system_message_handlers/DisplayMessageHandler.py::send_request()` | **Fixed (August 2026 re-analysis round, unbounded in-memory growth sweep).** `send_request()` stores `self.pending_requests[request_id] = {...}` before every send, but the only other reference to `pending_requests` anywhere in this 2700+ line file is a `del` on the *send-failure* branch -- there is no success-path cleanup. Investigating further revealed this class's separate `response_handlers`/`register_response_handler()`/`handle_response()` mechanism, which looked like it might be the actual response-correlation path instead, is *also* dead code: a repo-wide grep found zero callers of `register_response_handler()` or `handle_response()` anywhere. So neither mechanism actually correlates responses in this class today -- every successful `send_request()` call left a permanent, unremovable entry in `pending_requests` for the life of the process. Rather than fabricate a full TTL/response-handling system that doesn't otherwise exist here, applied the same bounded-cap-and-trim pattern already used successfully elsewhere in this exact file for `self._processed_request_ids` (cap at 1000, trim to the 500 most-recent-by-timestamp) -- stops the unbounded growth without inventing new response-handling behavior. Verified the trim logic in isolation (1500 sequential inserts correctly capped at <=1000, most-recent entries retained) and confirmed the class still constructs and behaves correctly live. Also found and documented (not fixed, no reachable caller): `Interfaces/userInterface/displays/radar/radar_event_system/radar_event_manager.py` and `.../radar_data_handler/base_radar_data_handler.py` have `self.pending_requests`-style dicts too, but both classes were confirmed to have zero instantiations anywhere in the codebase (`RadarEventManager`/`BaseRadarDataHandler` are both unreachable dead code, same category as `MissionManagementSystem`/`Futuristic*` displays documented earlier), so their versions of this pattern have no live impact. |
| `EventBus` subscriber callbacks invoked directly on GUI widgets from a background thread -- Qt cross-thread access hazard | `core/event_driven_communication.py::EventBus`, `Interfaces/userInterface/displays/radar/weather_radar_widget.py`, `Interfaces/userInterface/displays/holographic_mfd.py` | **Fixed (August 2026 re-analysis round, Qt thread-safety sweep), live-verified.** `EventBus._process_events()` runs on its own dedicated `"EventBus_Processor"` background thread and calls every subscribed callback synchronously via `callback(event.data)`. Two of this session's five confirmed-live subscribers are `QWidget` methods: `WeatherRadarWidget` subscribed `lambda _: self.update()` to `'weather_radar_update'`, and `HolographicMFD` subscribed `self._handle_mode_event` (which mutates `self.display_mode`, can construct new child widgets, and calls `self.update()`) to `'set_mfd_mode'`. Both topics are confirmed actively published from live code paths (`display_message_router.py`, both weather radar display classes) -- this was a real, reachable cross-thread GUI-object-access hazard, which Qt documents as unsupported/undefined-behavior-prone, not merely something that happened to work by luck. First attempted `QMetaObject.invokeMethod(..., QueuedConnection)` for the simpler no-argument `update()` case, but couldn't positively confirm live that its string-based method resolution actually reaches `update()`'s overloads in this PyQt6 build (the call's return value came back `None` even for a correctly-formed main-thread call in an isolated test). Switched both to `pyqtSignal`-based marshaling instead -- PyQt's own primary, best-documented mechanism for this exact case, since a signal emitted from a different thread than its receiver is automatically delivered via a queued connection. Live-verified via `QThread.currentThread()` comparison in both cases: publishing the event from an explicit background `threading.Thread` and then observing which `QThread` object actually executed `update()`/`_handle_mode_event()` -- both now run on the main GUI thread (confirmed identical `QThread` object identity), not the emitting background thread, with zero crashes across repeated runs. The other three `EventBus` subscribers found in the same sweep (`RadarEventManager`'s two, `BaseRadarDataHandler`'s one) are not `QWidget`s and were confirmed unreachable dead code in the async-correctness sweep below, so they don't need this fix. |
| `EventBus` subscriber callbacks that are `async def` are invoked synchronously and never awaited -- coroutine silently never runs (confirmed unreachable, not fixed) | `Interfaces/userInterface/displays/radar/radar_event_system/radar_event_manager.py`, `Interfaces/userInterface/displays/radar/radar_data_handler/base_radar_data_handler.py` | **Found (August 2026 re-analysis round, async-correctness sweep), documented rather than fixed.** `EventBus._handle_event()` calls `callback(event.data)` directly (no `await`, since it runs on a plain `threading.Thread`, not an asyncio loop). `RadarEventManager` subscribes `handler._handle_event` and a `_handle_display_update` lambda -- both `async def` -- to the bus this way; calling an `async def` function without awaiting it only constructs a coroutine object and immediately discards it, so the body never executes at all (Python doesn't start running a coroutine until something awaits or schedules it). Confirmed via repo-wide search that this is not reachable in practice: `RadarEventManager` and `BaseRadarDataHandler` are never instantiated anywhere, and nothing imports their modules except themselves -- the same confirmed-dead-code category as `MissionManagementSystem`/`Futuristic*` displays documented elsewhere in this file. Not fixed, since properly supporting async callbacks in `EventBus` (e.g. via `asyncio.run_coroutine_threadsafe()` against a live loop reference) would be new functionality for code that currently has no live caller, not a verification-driven fix to something broken in the running app. |
| `DataLink`/`Radio`/`SatCom` (the standalone `Systems/comms/{dataLink,radios,satcom}/*.py` classes, not the wired-in `CommsService`) have two stacked, compounding bugs -- confirmed unreachable, not fixed | `Systems/comms/dataLink/data_Link.py`, `Systems/comms/radios/radio.py`, `Systems/comms/satcom/satCom.py` | **Found (August 2026 re-analysis round, SQL-safety sweep), documented rather than fixed.** While auditing DBM.py's query construction for injection risk (conclusion: safe -- every f-string-interpolated identifier is either a hardcoded constant or, in `DatabaseManager.execute_query()`'s test-only table-verification path, extracted via a regex anchored to `[a-zA-Z_][a-zA-Z0-9_]*`, which can't match SQL metacharacters), found two real, live-reproducible bugs in these three files' constructors: (1) all three call `DatabaseManager(...).get_system_db('comms')`, but `dbConfig.xml` has no `'comms'` system -- only `'radio'`, `'data_link'`, and `'sat_com'` (confirmed live: constructing `DataLink()` raises `ValueError: [DBM] No database configured for system: comms` immediately); (2) `_setup_database()` in `data_Link.py`/`radio.py` calls `self.db.create_table(table_name, received_from, information_type, field_data_dict)` -- 4 arguments -- against `SystemDatabase.create_table(self, table_name, fields)`, which only accepts 2; this second bug is currently masked by the first (construction never reaches `_setup_database()`), but would itself raise `TypeError` even if bug (1) were fixed in isolation. Confirmed via repo-wide search these three classes are never instantiated anywhere except their own `if __name__ == "__main__":` demo blocks -- the live, wired-in comms implementation is the separate `CommsService` class in `Systems/comms/messaging_service.py` (which correctly uses `get_system_db('radio')` and is the one already fixed for the `time.sleep(5)` interruptibility bug earlier this round), simulating SatCom/DataLink/Radio state internally rather than delegating to these three classes at all. Same confirmed-dead-code, superseded-duplicate-implementation category as `MissionManagementSystem`/`ElectricalPowerController` documented elsewhere in this file -- not fixed, since there is no live caller for these bugs to affect. |
| ~~Spurious `RadarMain thread ... detected unhealthy system` ERROR on every clean run~~ | ~~`Systems/radarManagement/radarControl.py::RadarManagementSystem._update_loop()`~~ | **Fixed (Aug 21 2026 final pass).** The loop evaluated `is_healthy()`, which returns `self.running and radar_health`. Once a shutdown cleared the `_running` event this returned False for a perfectly healthy system, and the loop reported the normal stop as a fault at ERROR. Because a shutdown can flip the flag while an iteration is already in flight (past the `while self.running` guard), it fired on essentially every run — **live-traced at ~107 ms after the NORMAL -> SHUTTING_DOWN transition**, and it was the last ERROR left in an otherwise clean boot log, so every clean run still looked like it had a radar fault. `running` is now re-checked first and handled separately: a normal stop exits quietly at INFO, while the ERROR is reserved for a radar that is genuinely unhealthy *while the system is still meant to be running*. Verified: **0 ERRORs across 3 consecutive 35s boots** (previously 1 every run). Covered by `Tests/test_radar_shutdown_health.py` (12 assertions), which asserts both directions — silencing the message without preserving real fault reporting would have been worse than the bug — and is verified non-tautological by removing the guard (3 assertions then fail). |
| ~~Intermittent `Error starting thread 'UserCLI_Input'/'UserCLI_Processing': threads can only be started once`~~ | ~~`Utils/common/thread_manager.py::start_thread()`~~ | **Fixed (Aug 21 2026 completion pass).** `start_thread()` guarded only on `is_alive()`, but a `threading.Thread` that has already run to completion is *not alive* while `.start()` on it still raises `RuntimeError: threads can only be started once`. The UserCLI input/processing threads exit early when no interactive stdin is attached (headless/offscreen runs), so a later start attempt hit a dead-but-already-started Thread and logged an ERROR. Now also guards on `thread.ident is not None` (None before the first start, set forever after), which distinguishes "never started" from "started and finished" without touching private state; the request is declined at DEBUG instead of erroring. Covered by `Tests/test_thread_and_cli_resilience.py`. |
| ~~`Exception occurred in get_commands` ended CLI input for the whole session~~ | ~~`Utils/debug/userCLI.py::get_commands()`~~ | **Fixed (Aug 21 2026 completion pass).** The entire `while not self.stop_threads` loop sat inside one `try/except`, so any single unexpected exception terminated the thread permanently — and because a `threading.Thread` cannot be restarted (see the row above), CLI command input was gone for the rest of the session with only one logged line to show for it. Each iteration is now contained individually: transient failures are logged and the loop continues, with a 10-consecutive-failure circuit breaker (counter resets when errors are >30s apart) that disables CLI input cleanly rather than hot-spinning. The EOF-on-stdin path remains handled inline as an expected headless condition, not an error. |
| Other `EventBus`-style pub/sub and cross-thread-into-Qt sweep (checked, no further findings) | `Interfaces/scenarios/scenarioEngine.py`, `manual_animation_timer.py`, display layer broadly | **Checked (August 2026 re-analysis round).** Followed up the `EventBus`/`WeatherRadarWidget`/`HolographicMFD` cross-thread fix earlier this round by checking for the same pattern elsewhere: no `Qt.ConnectionType.DirectConnection` usage anywhere in the repo (which would be the other way to accidentally force a dangerous same-thread-context call across threads); `manual_animation_timer.py` uses `QTimer` (safe, GUI-thread-owned) rather than a Python thread; of the two other files with their own `threading.Thread(` (besides the already-covered `EventBus`), `scenarioEngine.py`'s background thread never touches any Qt widget or import, and `node_based_pfd.py` was already confirmed dead/unreachable code earlier this session. No further cross-thread Qt hazards found. |
| MIL-STD-1553B metadata decode-path robustness (checked, no fix needed) | `MIL_STD_1553B/metadata_codec.py::decode_metadata()` and its 4 call sites | **Checked (August 2026 re-analysis round).** `decode_metadata()` validates word count and version before parsing, but a malformed/non-binary data word (e.g. `int(data_words[0], 2)` on a corrupted string) would raise inside its own broad `try/except Exception as e: logger.error(...); raise` -- it logs then re-raises rather than swallowing. Checked all 4 call sites across the codebase (`RT_msg.py`, `RT_socket.py`, `universal_message_extractor.py`, `display_metadata_decoder.py`): every one already has its own enclosing `try/except` around the call, consistent with this codebase's established broad-except pattern, so a malformed word would be logged and contained rather than crashing the owning thread. No live-reproducible unguarded crash found; bus traffic is internally simulated in this app rather than received from an untrusted external network, which further limits how malformed data could realistically reach this path. |
| Singleton `__new__` methods have an unguarded check-then-create race under concurrent first construction (TOCTOU, same vulnerability class as the already-fixed `start()` races) | `storage/DBM.py::DatabaseManager`, plus 8 more: `Systems/radarManagement/radar_messaging/handlers/vil_data_handler.py::VILDataHandler`, `local_messaging/routing/handlers/vil_data_handler.py::VILDataHandler`, `Systems/radarManagement/radar_messaging/handlers/precipitation_data_handler.py::PrecipitationDataHandler`, `local_messaging/routing/handlers/precipitation_data_handler.py::PrecipitationDataHandler`, `Utils/common/system_state_manager.py::SystemStateManager`, `local_messaging/routing/response_services/data_response_services/echo_top_response_service.py::EchoTopResponseService`, `Systems/radarManagement/radar_messaging/handlers/mode_change_handler.py::ModeChangeHandler`, `local_messaging/routing/response_services/data_response_services/precipitation_response_service.py::PrecipitationResponseService`, `local_messaging/routing/response_services/data_response_services/vil_response_service.py::VILResponseService` | **Fixed (August 2026 re-analysis round, Round 6, singleton thread-safety sweep), live-verified.** `DatabaseManager.__new__` (and 20 other classes repo-wide using the same `_instance = None` / `if cls._instance is None:` singleton pattern) creates the instance with no lock, unlike `EventBus.__new__` which already uses `with cls._lock:`. Live-reproduced with a 20-thread concurrent first-construction hammer test: with the GIL switch interval tightened (`sys.setswitchinterval(1e-7)`) to widen the race window, 1 of 30 trials produced 2 distinct `DatabaseManager` instances from the same class-level `_instance` slot. Fixed with the same double-checked-locking pattern `EventBus` already uses; re-ran the identical 30-trial hammer test post-fix with 0/30 producing more than 1 instance. Of the 20 other unguarded singletons found in the same repo-wide sweep, 9 (listed left) are confirmed live via real cross-file imports/instantiations (not just their own `if __name__ == "__main__":` block) and got the identical fix; the remaining 11 (`BlockTransferManager`, `RadarMessenger`, `RadarManagementSystem`, `UserCLI`, `MessageRoutingService`, `DisplayMessenger`, `DisplayManager`, `FMSAttitudeResponseService`, `FMSNavigationResponseService`, `FMSVelocityResponseService`, `FMSTacticalResponseService`, `EnhancedRadarResponseService`) were confirmed to have zero import statements anywhere outside their own defining file -- same confirmed-dead-code, superseded-duplicate-implementation category documented elsewhere in this file -- and were left unfixed. Note: for the 9 fixed classes that guard their own `__init__` body with `if not self._initialized:` (all except `SystemStateManager`, which does its one-time init inside the now-locked `__new__` itself), only object *creation* in `__new__` was locked, not the `__init__` body itself; `DatabaseManager` is the one exception where `__init__`'s body was also wrapped in the lock, since two threads sharing the same instance could still both observe `_initialized == False` and both run `__init__`'s body concurrently. Left as a documented, narrower residual risk for the other 8 (not reproduced live this round) rather than wrapping retry-loop/DB-table-creation logic in a lock without a demonstrated failure to verify against. |
| Fire-and-forget `asyncio.Task`s created via `loop.create_task()` in the boot and shutdown paths are never awaited or have their exceptions retrieved -- a failure surfaces only via asyncio's generic default handler at an unpredictable future GC time, with no application-level context | `core/system_manager.py::start_FM_system()` (`init_task`, `start_components_task`), `core/system_manager.py::stop_system()` (6 `stop_task`/`radar_management.stop()` sites) | **Fixed (August 2026 re-analysis round, Round 6, asyncio task exception-visibility sweep), live-verified.** Live-reproduced the underlying gap in isolation first: a `create_task()`'d coroutine that raises, with the returned Task never awaited or referenced again, only produces a generic "Task exception was never retrieved" log line (via the `'asyncio'` logger, not this app's own `logger`) once the Task object is garbage-collected -- confirmed live by forcing GC immediately after the failure and observing the delayed, context-free log message. `system_manager.py` has 8 such fire-and-forget task sites: 2 in the boot path (`init_task` for `initialize_system()`, `start_components_task` for `start_async_components()` -- both boot-critical) and 6 in the shutdown path (`radar_management.stop()` plus 5 `stop_task` sites covering `display_response_service`, `vil_response_service`, `routing_service`, `async_handler`, and the reversed-order "remaining components" loop). None of the 8 had any exception retrieval at all -- unlike the structurally identical pattern already used correctly in the `response_services` family (`vil_response_service.py` etc.), which pairs every `create_task()` with `task.add_done_callback(self._handle_task_done)`. Fixed by adding a small `SystemManager._log_fire_and_forget_task_exception(task, task_name)` helper (retrieves `task.exception()`, guards against `CancelledError`, logs through this module's own `logger` with the failing task's name) and attaching it via `add_done_callback` at all 8 sites -- the same pattern already proven correct elsewhere in this codebase, not a new mechanism. Verified: ruff clean, full 173-test suite passes, and a full boot+SIGTERM test (this time polled every 3s for 30s to rule out the flaky single-snapshot check used in earlier rounds) shows the process alive and stable the entire window, then a clean SIGTERM-triggered exit with 0 ERROR/CRITICAL/traceback lines. Separately noted but **not fixed**: the 6 shutdown-path `stop_task`s are created inside the synchronous `stop_system()` method and only get a chance to actually *run* (not just have exceptions retrieved) during the caller's subsequent `await asyncio.sleep(0.1)` in `Main.py::shutdown()` -- if any given component's async `stop()` body takes longer than its slice of that 100ms window, its cleanup could be truncated when the process exits shortly after. Making `stop_system()` properly `async def` and `await`ing an `asyncio.gather()` of all six stop tasks would close this gap, but is a larger structural change than this round's scope; the repeated 30s boot+SIGTERM tests this round showed no observable symptom of it (0 errors, clean exit every time), so it's documented as a latent risk rather than fixed without a demonstrated failure to verify against. |
| Two radar display classes create unparented `QTimer()`s that are never explicitly stopped -- `cleanup()` (called by `RadarDisplayFactory` on display-type cache eviction) either misses some of them or doesn't exist at all | `Interfaces/userInterface/displays/radar/weather_radar_holographic_display.py::WeatherRadarHolographicDisplay` (+ its composed `RadarSelectionMenu`), `Interfaces/userInterface/displays/radar/weather_radar_display.py::WeatherRadarDisplay` | **Fixed (August 2026 re-analysis round, Round 6, QTimer/QPixmap resource-lifecycle sweep), live-verified.** Repo-wide sweep found 24 `QTimer()` instantiations across 19 files; 18 of the 24 are unparented (no `self` passed as Qt parent, so no automatic C++-side cleanup on widget destruction). Live-reproduced in isolation first that this is largely non-fatal on its own: constructing `WeatherRadarHolographicDisplay()`, dropping every Python reference, and calling `gc.collect()` fully reclaims the widget and its running timers even with zero explicit `.stop()` calls, because Python's cyclic garbage collector correctly breaks the widget->timer->bound-method->widget reference cycle PyQt6 signal/slot connections create. Most of the 18 unparented-timer classes (PFD/MFD/EICAS/TSD/SMS family) are created once at boot and live for the app's lifetime, so this pattern has no practical impact there. The one place display widgets actually get dynamically created and discarded during live operation is `RadarDisplayFactory.create_display()`'s `_display_cache`, which calls `cached_display.cleanup()` (if present) then an explicit `gc.collect()` when a radar's display type switches between holographic and standard. Checked both classes reachable through that path for weather radar: `WeatherRadarHolographicDisplay` creates 4 QTimers total (`_animation_timer` on the base class, plus its own `_manual_animation_timer`/`_cleanup_timer`, plus its composed `RadarSelectionMenu`'s `_animation_timer`/`_pulse_timer`) but inherits `FuturisticRadarDisplay.cleanup()` unmodified, which only knows about the one `_animation_timer` it defines itself -- confirmed live the other 3 were still `.isActive() == True` after calling `cleanup()`. `WeatherRadarDisplay` (the standard, non-holographic variant) had no `cleanup()` method anywhere in its class hierarchy at all, so `hasattr(cached_display, 'cleanup')` was `False` and the factory's cleanup step silently no-op'd for it, leaving `_cleanup_timer`/`_stats_timer` running with cleanup relying entirely on the factory's `gc.collect()` call. Fixed by adding a `cleanup()` override to `WeatherRadarHolographicDisplay` (stops all 3 of its own/composed timers, then calls `super().cleanup()`) and a new `cleanup()` method to `WeatherRadarDisplay` (stops its 2 timers; does not touch `_animation_controller`/`_spatial_grid`/`_dirty_region_tracker`, which are shared `get_*()` singletons, not instance-owned). Live-verified: constructed both classes, confirmed the relevant timers were active before `cleanup()` and every one of them `.isActive() == False` immediately after, no exceptions raised. Other radar mode types (SAR/TFR/Targeting/AEWC) go through the same factory path but use the generic `HolographicRadarDisplay` base class directly (no mode-specific timer subclass like weather radar's), so they only have the one already-handled `_animation_timer` -- confirmed via `find` that `weather_radar_holographic_display.py` is the only file of its kind. Separately checked the other half of this task, `QPixmap` reuse: zero `QPixmap(` usage anywhere in the repo (rendering is done via direct `QPainter` calls, not a pixmap-caching layer). The one image-buffer class that does exist, `rendering/weather_data_buffer_manager.py::WeatherDataBufferManager`, already lazily creates each `QImage` buffer once (`if data_type not in self.buffers:`) rather than reallocating per frame -- no fix needed there. The five `QImage(...)` calls in `weather_radar_holographic_display.py` (`_generate_*_texture()`) are all one-time calls from `_initialize_textures()` in `__init__`, not per-frame. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 5s for 25s) shows the process alive and stable the entire window, then a clean SIGTERM-triggered exit with 0 ERROR/CRITICAL/traceback lines. |
| Round 7 sweep (QPainter lifecycle, MIL-STD-1553B BC/RT concurrency, ThreadPoolExecutor exhaustion) -- all three angles checked out clean, documentation-only round | `Interfaces/userInterface/displays/base_display.py`, `.../radar/weather_radar_widget.py`, `.../radar/weather_radar_holographic_display.py`, `.../rendering/weather_data_buffer_manager.py`, `MIL_STD_1553B/Bus_Controller/BC.py`, `MIL_STD_1553B/Remote_Terminal/RT.py`, `MIL_STD_1553B/Bus_Controller/BC_transfer_aggregator.py`, `storage/DBM.py` | **Checked (August 2026 re-analysis round, Round 7), live-verified, no fix needed.** **QPainter lifecycle:** `base_display.py::paintEvent()` (the base class most displays inherit) already uses a careful explicit `try/finally` with `painter.isActive()` before `painter.end()` -- exemplary. Three other files construct `QPainter` directly (`weather_radar_holographic_display.py`'s five one-time `_generate_*_texture()` calls, `weather_data_buffer_manager.py::composite_layers()`, `weather_radar_widget.py::paintEvent()`) without an explicit `.end()` on every path, relying instead on Qt/PyQt6's RAII-style destructor behavior (a `QPainter`'s C++ destructor calls `end()` automatically when the wrapping Python object is deterministically refcounted to zero -- not the same fragile cyclic-GC dependency the QTimer finding earlier this round had, since a bare local variable has no reference cycle and CPython refcounting reclaims it immediately on function return). Live-reproduced a 200-cycle `repaint()`/`processEvents()` stress test against `WeatherRadarWidget` (whose `paintEvent()` never calls `.end()` at all, the most exposed case): zero Qt-level warnings (e.g. "QPainter::begin: A paint device can only be painted by one QPainter at a time") across all 200 cycles, and zero such warnings appear anywhere across the dozens of boot-test logs captured this session. No fix applied. **BC/RT concurrency:** both `Bus_Controller` (`BC.py`) and `Remote_Terminal` (`RT.py`) declare a `pending_requests = {}` dict with an accompanying lock (`self._lock` / part of `_instance_lock`), apparently intended for thread-safe request tracking -- confirmed via repo-wide grep that neither dict is EVER populated with an assignment anywhere in the codebase (`BC.py`'s copy has dead read/delete branches referencing it inside `_process_single_frame()`; `RT.py`'s copy has zero references at all beyond its own declaration) -- both are vestigial, same confirmed-dead-code category as other findings this session. `Bus_Controller.is_listening` is a second, separately dead flag (set once at init, never read or written again; the real flag is `self.listening`). The one shared structure that IS genuinely live, `Remote_Terminal.processed_messages` guarded by `self.message_lock`, was confirmed correctly and consistently locked at all 6 access points. `BlockTransferAggregator.pending_transfers`/`transfer_timeouts` (`BC_transfer_aggregator.py`) has no lock at all and IS genuinely populated/mutated, but tracing all three mutating methods (`register_message()`, `_finalize_transfer()`, `_cleanup_expired_transfers()`) back to their only caller chain shows they're invoked exclusively via `BC.py`'s `process_frame()` coroutine, which is only ever run via `loop.run_until_complete(self.process_frame(...))` inside the single dedicated BC listener thread's own while-loop (same pattern confirmed for RT's mirror-image `RT_transfer_aggregator.py`) -- fully serialized by construction, not by locking, so no race exists despite the missing lock. The separate `BC_sender`/`RT_sender` classes (`BC_socket.py`/`RT_socket.py`) do use a `ThreadPoolExecutor(max_workers=5)` for message sends, but the only aggregator methods they call (`is_binary_data_message`, `extract_binary_data`, `_is_precipitation_message`, `_is_vil_message`) don't touch `pending_transfers`/`transfer_timeouts` at all -- confirmed by reading all five method bodies. **ThreadPoolExecutor exhaustion:** found 4 pools repo-wide (`DatabaseManager.worker_pool`, `BC_sender`/`RT_sender`'s `max_workers=5` each, `AsyncMessageHandler`'s `max_workers=self.num_workers` default 4). `BC_sender.BC_send_message()`/`RT_sender`'s equivalent call `future.result()` immediately after `.submit()`, blocking the caller until the worker finishes -- naturally self-throttling, not a silent-drop or unbounded-queue risk. `DatabaseManager.execute_query_async()`/`process_batch()` are genuinely fire-and-forget (Future never retrieved) and reachable from live code (confirmed via a code comment in `powerManagementSystem.py::_setup_database()` referencing this exact path as "now load-bearing since this class is wired into the live boot sequence") -- structurally the same bug class as the asyncio fire-and-forget tasks fixed earlier this round, but live-reproduced submitting a guaranteed-to-fail query (`INSERT INTO this_table_does_not_exist_at_all`) via `execute_query_async()` and waiting out all 3 retry attempts: every single attempt, including the final one, is already fully logged with query text, params, system name, and full traceback by `execute_query()`'s own internal `try/except` -- unlike the asyncio case (which had zero internal logging before this round's fix), the failure is never actually silent here despite the Future itself being discarded, so no fix was needed. `max_total_workers` is configured at 20, shared across all ~16 configured DBM systems -- no evidence of queue backlog in any boot test this session. `AsyncMessageHandler` uses dedicated named worker threads consuming an internal queue (plus a watchdog thread) rather than a submit-per-task pattern, a more elaborate design already covered by the general unbounded-growth sweep done in an earlier round. |
| `Flight_Management_Operating_Flight_Program.shutdown()` had a TOCTOU race allowing `system_manager.stop()` to run twice if two shutdown-triggering signals (e.g. duplicate SIGTERM, or SIGINT then SIGTERM) arrived close together | `Main.py::Flight_Management_Operating_Flight_Program.shutdown()` | **Fixed (August 2026 re-analysis round, Round 8, stop()/cleanup()/shutdown() idempotency sweep), live-verified.** `shutdown()` is scheduled via `loop.call_soon_threadsafe(lambda: asyncio.create_task(fmofp.shutdown()))` from the SIGINT/SIGTERM signal handler in `start_fmofp()` -- two signals arriving close together each schedule their own `shutdown()` task. The guard at the top (`if not self._started: return`) only protects against a second signal arriving after the first shutdown() has *fully finished*: the flag flip (`self._started = False`) was left until after `self.system_manager.stop()`, `await asyncio.sleep(0.1)`, and `self.system_manager.wait_for_shutdown()` had all already run, so a second `shutdown()` task starting during that window would still see `_started == True`, pass the guard, and call `self.system_manager.stop()` a second time on top of the first. Live-reproduced with a stand-in class mirroring the exact guard/synchronous-work/await structure: two overlapping `shutdown()`-equivalent calls both passed the guard and both invoked the stop-work (call counter confirmed 2, not 1). Fixed by moving `self._started = False` to immediately after the guard check, before any of the real shutdown work or the first `await` -- re-ran the identical stand-in reproduction post-fix: call counter confirmed 1, second call correctly short-circuited via the guard. Also spot-checked (defense in depth, not exhaustive): `BC_sender`/`RT_sender.shutdown()` -- safe to call twice, since `ThreadPoolExecutor.shutdown()` is documented as idempotent and the rest of the method only sets a flag and an `Event`, both idempotent; `DatabaseManager.shutdown()` -- same `ThreadPoolExecutor.shutdown()` safety net for its `worker_pool`. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 5s for 20s, then two SIGTERM signals sent back-to-back with no delay) shows clean exit with 0 ERROR/CRITICAL/traceback lines and no hang. |
| Repo-wide sweep for subprocess/os.system/eval/exec/pickle/unsafe-yaml usage (checked, no findings) | whole repo | **Checked (August 2026 re-analysis round, Round 8).** Zero matches anywhere in the codebase for `subprocess.`, `os.system(`, `os.popen(`, bare `eval(`, bare `exec(`, `pickle.`, or `yaml.load(` -- the one `exec(` -adjacent hit was a comment referencing `QApplication.exec()` (a legitimate PyQt method), not an actual dynamic-code-execution call. No injection or unsafe-deserialization surface exists in this codebase at all. |
| JSON serialization edge cases in MIL-STD-1553B socket message passing (checked, no fix needed) | `MIL_STD_1553B/Bus_Controller/BC_connect/BC_socket.py`, `MIL_STD_1553B/Remote_Terminal/RT_connect/RT_socket.py` | **Checked (August 2026 re-analysis round, Round 8).** Neither file passes `allow_nan=False` to any `json.dumps()` call, so a `NaN`/`Infinity` float reaching a message (e.g. from a division-by-zero-adjacent physics calculation) serializes fine as a non-standard-but-Python-parseable JSON token rather than raising -- and since both send and receive ends are this same Python `json` module (an internal simulation, not an interop boundary with a strict external JSON consumer), it round-trips without error. Checked the receive-side `json.loads()` call sites in both files for malformed/truncated-input robustness: `BC_socket.py`'s is wrapped in `except (ValueError, SyntaxError) as e:` (a `json.JSONDecodeError` is a `ValueError` subclass) which logs `"Failed to parse frame data: {e}"` and lets the listener loop continue rather than crashing the connection; `RT_socket.py`'s equivalent call sites are wrapped in broad `except Exception as e:` handlers with the same continue-on-error behavior. No live-reproducible crash or silent-corruption path found. |
| BC/RT socket listeners could silently drop any received message whose JSON-encoded size exceeded the fixed 1024-byte `recv()` buffer -- a message sent via a single `sendall()` could arrive split across multiple `recv(1024)` calls, and each fragment was parsed independently instead of being reassembled | `MIL_STD_1553B/Bus_Controller/BC_connect/BC_socket.py::BC_Listener.handle_connection()`, `MIL_STD_1553B/Remote_Terminal/RT_connect/RT_socket.py::RT_Listener.handle_connection()` | **Fixed (August 2026 re-analysis round, Round 9, socket recv() partial-read robustness), live-verified.** Both listener methods called `data = connection.recv(1024)` once per loop iteration and immediately attempted `json.loads()` on that single chunk -- no accumulation buffer across `recv()` calls, no length-prefixed or delimited message framing. TCP is a byte stream, not message-oriented, so it makes no guarantee that one `sendall()` on the sender maps to one `recv()` on the receiver. Live-reproduced with a real TCP client/server pair mirroring the exact receive loop: a 2133-byte message (a realistic size for a full 32-data-word MIL-STD-1553B block-transfer chunk plus metadata -- the sender's own `_send_large_message()` caps chunks at 32 data words for protocol-limit reasons, not to fit a 1024-byte TCP buffer) sent via one `sendall()` arrived split across 3 separate `recv(1024)` calls; each fragment independently failed `json.loads()` ("Failed to parse frame data") and the entire message was silently lost -- not corrupted into wrong data, gone entirely, with only a terse per-fragment parse-error log line as any trace. Fixed by accumulating raw bytes across `recv()` calls into a per-connection buffer, decoding and trial-parsing the *accumulated* buffer each time instead of just the latest fragment, and only proceeding to the existing (unmodified) processing logic once a complete JSON message is present -- otherwise looping back for more data. Bounded by a 10 MB safety cap per connection against a truly malformed/never-terminating stream. The existing non-JSON "single frame format" fallback branch is unaffected (it never reaches the JSON trial-parse). Live-verified by re-running the same 2133+ byte reproduction post-fix: the full message now appears intact in the "Decoded data" log line with zero parse failures; also re-verified a small (76-byte, single-`recv()`) message still works identically to before, confirming no regression for the common case. Same fix mirrored in `RT_Listener.handle_connection()`, independently live-verified the same way. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 5s for 25s) shows clean stable boot and clean SIGTERM-triggered exit, 0 ERROR/CRITICAL/traceback lines. |
| numpy dtype overflow/precision edge cases in physics/radar calculations (checked, no fix needed) | whole repo | **Checked (August 2026 re-analysis round, Round 9).** Found only one narrow fixed-width integer dtype cast anywhere in the codebase -- `syntheticAperture/sar_radar.py`'s `np.clip(image + noise, 0, 255).astype(np.uint8)` -- and it's already correctly clamped to the valid `uint8` range *before* the cast, so no silent wraparound is possible. Zero `np.float32` usage anywhere in the repo -- all numpy float arithmetic uses Python's default `float64`, so there's no float32/float64 mixing or precision-loss risk to check either. |
| BC/RT listener connections weren't guaranteed to be closed if `handle_connection()` ever raised a `BaseException` its own broad `except Exception` doesn't catch (defense-in-depth hardening, not a live-reproduced bug) | `MIL_STD_1553B/Bus_Controller/BC_connect/BC_socket.py::BC_Listener.start_listening()`, `MIL_STD_1553B/Remote_Terminal/RT_connect/RT_socket.py::RT_Listener.start_listening()` | **Hardened (August 2026 re-analysis round, Round 10, socket leak sweep).** `handle_connection()` already closes its `connection` on every normal and caught-exception exit path (its own `except Exception as e: ... break` followed by `connection.close()` after the loop), so the accept loop that calls it (`self.handle_connection(connection, client_address)`, itself inside a broader `except Exception` in `start_listening()`) is very unlikely to actually leak in practice -- `except Exception` catches essentially everything realistic, including `RecursionError` and `MemoryError`. The only gap is a `BaseException` that isn't an `Exception` subclass (`SystemExit`, `KeyboardInterrupt`, `GeneratorExit`) landing inside this background listener thread, which essentially never happens outside of an explicit `sys.exit()` call from within the thread. Not a live-reproduced bug, but added a `try/finally` around the `handle_connection()` call in both listeners' accept loops with an idempotent `connection.close()` in the `finally` (closing an already-closed socket is a documented no-op) as low-risk, purely-additive defense in depth, rather than leaving a theoretical gap in place with no real cost to closing it. Send-side (`BC_sender`/`RT_sender._send_message()`) was already confirmed safe -- both use `with socket.socket(...) as s:`, which Python's context manager protocol guarantees closes on every exit path including exceptions. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 5s for 25s) shows clean stable boot and clean SIGTERM-triggered exit, 0 ERROR/CRITICAL/traceback lines. |
| Radar/nav bearing and heading angle-wraparound math (checked, no fix needed) | `Systems/missionPlanning/routeManagement/handleRoute.py`, `Systems/nav/ins/inertialNavigationSystem.py`, `Systems/nav/gps/globalPositionSystem.py`, `Systems/radarManagement/weather/weather_state_manager.py`, `Systems/radarManagement/radar_data_fusion.py`, `Systems/flightManagementSys/fmsControl.py`, `Systems/flightManagementSys/flightControlSys/attitude_calculator.py`, `Interfaces/userInterface/displays/holographic_mfd.py`, `Interfaces/userInterface/displays/tsd.py`, `Interfaces/userInterface/displays/radar/targeting_radar_display.py` | **Checked (August 2026 re-analysis round, Round 11, angle wraparound sweep), no fix needed.** Swept every `atan2`/heading/bearing computation repo-wide for missing modulo normalization -- a class of bug that would show up as e.g. a bearing suddenly jumping from 359 deg to -1 deg instead of wrapping to 0-360, or a targeting solution silently drifting after repeated deltas. Every bearing/heading value that is stored, displayed, or fed back into further additive state (route bearing, INS heading integration, weather-cell azimuth tracking, radar fusion bearing, FMS target heading, roll/yaw attitude) is already correctly normalized at its point of computation -- `% 360` for 0-360 bearings, `((x + 180) % 360) - 180` for signed roll. Two `atan2` results (`holographic_mfd.py`'s `threat_angle`, `targeting_radar_display.py`'s velocity-vector `angle`) are left unnormalized in radians, but both are confirmed to only ever feed straight into `math.cos()`/`math.sin()` calls for a one-shot vector endpoint each frame -- trig functions are periodic, so an unnormalized angle in that specific context is mathematically equivalent to a normalized one, not a live bug. No fix applied. |
| `EventBus.subscribe()` had no counterpart method anywhere in the class -- every subscription was permanent for the life of the process, and the two `QWidget` subscribers fixed for cross-thread safety earlier this session (`WeatherRadarWidget`, `HolographicMFD`) are both reachable through display-factory cache-eviction paths that destroy and recreate the underlying widget instance, leaking one stale callback into `EventBus.subscribers` every time | `core/event_driven_communication.py::EventBus`, `Interfaces/userInterface/displays/radar/weather_radar_widget.py`, `Interfaces/userInterface/displays/holographic_mfd.py` | **Fixed (August 2026 re-analysis round, Round 11, EventBus subscriber leak sweep), live-verified.** `EventBus.subscribe()` appends a callback to `self.subscribers[topic]` under `self._lock`, but no `unsubscribe()` or equivalent ever existed anywhere in the class -- confirmed via repo-wide grep. `WeatherRadarWidget` (via `WeatherRadarDisplayFactory`'s `get_weather_radar_widget(force_reset=True)` reset path) and `HolographicMFD` (via `MFDDisplayFactory`'s cache-eviction path that calls the evicted instance's `cleanup()`) are both genuinely recreated during live operation -- the same display-cache-recreation pattern documented earlier this session for the QTimer leak fix -- and each `__init__` call added one more permanent entry to `EventBus.subscribers['weather_radar_update']` / `['set_mfd_mode']` respectively, with the old widget's callback still referenced by the bus even after the widget itself was otherwise fully dereferenced everywhere else, keeping it alive indefinitely and causing stale/duplicate UI updates to accumulate with every display-type toggle. Live-reproduced first: tracked `len(event_bus.subscribers['weather_radar_update'])` across repeated `force_reset=True` calls and confirmed it grew by 1 each time with no way to shrink it back down. Fixed by adding `EventBus.unsubscribe(topic, callback)` (removes the callback under the same lock, returns True/False rather than raising if already absent, matching this codebase's established not-found-is-not-an-error convention for cleanup paths), then wiring both widgets to store their subscribed callback as a named instance attribute (`self._weather_update_callback`, `self._mfd_mode_callback`) instead of an anonymous inline lambda/bound-method expression, so the exact same callback object can be looked up and removed later. `WeatherRadarWidget`'s factory reset path now calls `unsubscribe()` on the outgoing instance before the existing cache-clear logic runs; `HolographicMFD`'s own `cleanup()` (already invoked by `MFDDisplayFactory` on cache eviction, already housing the QTimer-equivalent cleanup for this class) now also unsubscribes at its start. Live-verified both with explicit subscriber-count tracking across repeated create/reset or create/cleanup cycles: `WeatherRadarWidget` count held steady at 1 across two consecutive `force_reset=True` resets (previously would have grown to 3), with a `weakref` check confirming the old widget instance is now actually garbage-collected after reset rather than pinned alive by the stale subscription; `HolographicMFD` showed the exactly-expected balanced sequence across three create/cleanup cycles (0 to 1 to 2 to 1 to 2 to 1 to 0), never drifting upward. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 ERROR/CRITICAL/traceback lines (the only 4 `error`-matching lines in the boot log are filenames like `RT_error_check.py`/`weather_radar_error.py`, not actual error log entries). |
| `DisplayNode._notify_subscribers()` iterated the live `self.subscribers` set with no locking, and `DisplayNode.update()` held a plain `threading.Lock` across an `await` -- two stacked concurrency bugs in the display-tree pub/sub system (separate from `EventBus`, used by weather radar's node-based state tree) | `Interfaces/userInterface/displays/display_nodes/display_node_base.py::DisplayNode` (`update()`, `_notify_subscribers()`), reachable via `Interfaces/userInterface/displays/radar/weather_radar_display.py` and `Interfaces/userInterface/displays/radar/weather_radar_widget.py` | **Fixed (August 2026 re-analysis round, Round 12, display-tree subscriber race/deadlock sweep), live-verified.** Followed up on Round 11's note that `weather_radar_widget.py`'s reset logic references a second, separate subscriber system (`DisplayNode.subscribers`, distinct from `EventBus`) that hadn't itself been audited yet. Confirmed this system is genuinely live (not dead code): `weather_radar_display.py::_setup_node_subscribers()` calls `add_subscriber()` on the weather-radar node tree during display construction, and `display_tree_manager.py` drives `update()` calls on those same nodes as radar data flows in. Found two stacked bugs. (1) `_notify_subscribers()` iterated `self.subscribers` (a live, mutable `set`) directly with no lock at all, while `weather_radar_widget.py`'s widget-reset path (and, separately, `paintEvent()`'s own `_check_display_type_change()`) calls `visual_node.subscribers.clear()` / `type_node.subscribers.clear()` directly, bypassing even the class's own `remove_subscriber()` API. Live-reproduced with a real `DisplayNode`: an in-flight `update()` notifying several async subscribers concurrently with a `.subscribers.clear()` call raised `RuntimeError: Set changed size during iteration` in 10/10 runs. (2) Independently, `update()` wraps its entire body -- including `await self._notify_subscribers()` -- in `with self._lock:`, where `self._lock` is a plain `threading.Lock`, not an `asyncio.Lock`. Since asyncio is single-threaded, any concurrent call to `add_subscriber()`/`remove_subscriber()` (both of which also acquire `self._lock`) while an `update()`'s notify pass is suspended mid-await permanently deadlocks the entire event loop -- the lock can only be released by the same coroutine that's blocked waiting for the loop to schedule it, which can't happen while the loop thread itself is stuck on a synchronous `Lock.acquire()` call. Live-reproduced: a notifier task and a concurrent, properly-API-guarded `remove_subscriber()` call deadlocked every run (confirmed via `asyncio.wait_for()` timing out at 25s with zero progress, not merely slow). Fixed by (a) moving the `if notify: await self._notify_subscribers()` call outside the `with self._lock:` block in `update()` -- the lock still fully protects the synchronous value/metadata mutation, just not the async notification -- and (b) having `_notify_subscribers()` take a lock-protected snapshot (`list(self.subscribers)`) before iterating, so concurrent mutation of the live set (whether via the proper API or a direct bypass) can no longer affect an in-progress notification pass. Live re-verified both reproductions post-fix: the `.clear()`-during-notify race no longer raises (20/20 clean runs); the notifier/remove_subscriber deadlock no longer occurs, with all subscribers correctly invoked and both coroutines returning normally. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines. |
| ~~Benign diagnostics logged at WARNING drowned out real warnings~~ | ~~`Interfaces/userInterface/displays/radar/radar_display_data_coordinator.py`, `storage/DBM.py`, `display_message_router.py`, `display_tree_manager.py`~~ | **Fixed (Aug 21 2026 completion pass).** A 40s boot emitted **558 WARNING lines**, essentially none of which reported a problem: the radar data coordinator logged its periodic STORAGE/RETRIEVAL statistics dumps and a per-cycle "Finished processing: N valid items retained" line at WARNING (~150 lines), and `DBM.load_config()` logged "Duplicate configuration call for: X. Skipping." at WARNING every time its dedup guard worked as designed — 14 times per configured system, 238 lines. With real ERRORs now at zero, this noise was the main obstacle to reading the log. All of these are reclassified to DEBUG (17 call sites); genuine warnings — messenger/queue health, radar state conflicts, data-quality filtering — are untouched. **Measured: 558 → 109 warnings on an identical 40s boot, an 80% reduction**, with the remainder being low-volume lines that do carry signal. |
| `WeatherRadarWidget._check_display_type_change()` (called from the first line of `paintEvent()`) called `self.repaint()` immediately after `self.update()` to force a redraw of the newly-constructed display -- a forced, synchronous repaint issued from within an already-executing `paintEvent()` | `Interfaces/userInterface/displays/radar/weather_radar_widget.py::WeatherRadarWidget.paintEvent()` / `_check_display_type_change()` | **Fixed (August 2026 re-analysis round, Round 12, GUI-thread-blocking/repaint-reentrancy sweep), live-verified.** Swept for blocking/reentrant operations on the Qt GUI thread. `_check_display_type_change()` runs at the top of every `paintEvent()` call; its expensive branch (only taken when the configured display type has actually changed) constructs a brand-new display object and, at the end, called `self.update()` followed unconditionally by `self.repaint()`. `repaint()` forces an *immediate, synchronous* re-entry into `paintEvent()` before the current call has returned -- Qt explicitly treats this as illegal reentrancy. Live-reproduced with a minimal `QWidget` replicating the exact `update()`-then-`repaint()` sequence called from inside `paintEvent()`: Qt logs "QWidget::repaint: Recursive repaint detected" and still executes a second, nested `paintEvent()` call before the first returns, and a subsequent real (non-reentrant) paint pass afterward failed to construct a valid `QPainter` ("QPainter::begin: Paint device returned engine == 0", "Painter not active, aborted") -- not just a harmless warning, an actual broken render pass. Fixed by removing the redundant `self.repaint()` call; `self.update()` alone already achieves the intended goal (schedule a repaint of the new display) via Qt's normal, non-reentrant async-scheduling path, the same mechanism used everywhere else in this codebase to request a repaint. Live re-verified with the same minimal-widget reproduction: with only `self.update()`, `paintEvent()` exits cleanly, no "Recursive repaint detected" warning, no reentrant nested call. Separately profiled (documented, not fixed): constructing a fresh `WeatherRadarHolographicDisplay()` -- the expensive branch's other main cost -- takes ~200ms on average (5-run measurement: 183-231ms), executed synchronously on the GUI thread at the exact moment of a display-type toggle; `WeatherRadarDisplay()` (the standard variant) by contrast takes ~1ms. Unlike the reentrancy bug, there's no small, targeted fix for this -- `WeatherRadarWidget` builds a fresh display instance from scratch on every single toggle rather than caching/reusing previously-built instances the way `RadarDisplayFactory`/`MFDDisplayFactory` already do for the analogous other-radar-family widgets (documented earlier this session); retrofitting that same caching discipline here would require reworking this class's own subscribe/unsubscribe-on-transition logic to handle a reused (not freshly-constructed) instance correctly across arbitrary standard/holographic toggle sequences -- a larger structural change than this round's scope, left as a known, documented UX-latency issue (a one-time ~200ms stutter on toggle) rather than a correctness bug. Also confirmed (checked, dead code): the ten other `self.repaint()`/`paintEvent()`/`update()` occurrences found repo-wide during this sweep (five each in `weather_radar_holographic_display.py` and `weather_radar_display.py`) never execute -- live-confirmed via `isinstance(instance, QWidget)` returning `False` and `hasattr(instance, 'repaint')` returning `False` for both `WeatherRadarHolographicDisplay` and `WeatherRadarDisplay` (neither inherits from `QWidget`; their `paintEvent`/`repaint`-named methods are same-named-but-unrelated plain Python methods that Qt's dispatcher never invokes), so every `if hasattr(self, 'repaint'):`-guarded call among them is a permanently-false, dead branch -- same confirmed-dead-code category as other findings this session. The remaining three `self.repaint()`/`.repaint()` call sites found repo-wide (`mfd.py`, `holographic_settings_panel.py` x2) are all invoked from ordinary settings-change slot callbacks, not from within any active `paintEvent()`, and are safe. Also checked (documented, no fix needed): `displayMessenger.py::_monitor_message_task()`'s two `time.sleep()` calls are unreachable -- zero callers anywhere in the codebase, confirmed via repo-wide grep; `scenarioEngine.py`'s `time.sleep(0.1)` runs on its own dedicated background dispatch thread (established in an earlier round), not the Qt GUI thread. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines, 0 "Recursive repaint" warnings anywhere in the log. |
| `WeatherRadarWidget.paintEvent()` constructed a `QPainter` with no exception handling at all, while `draw_radar_elements()` (its deep rendering call tree) deliberately logs-and-re-raises rather than swallowing | `Interfaces/userInterface/displays/radar/weather_radar_widget.py::WeatherRadarWidget.paintEvent()` | **Fixed (August 2026 re-analysis round, Round 13, Qt exception-propagation sweep), live-verified.** Investigated what happens when a PyQt slot/virtual-method-override raises an uncaught exception. Confirmed this app installs a custom `sys.excepthook` (`Main.py::global_exception_handler`) that logs at CRITICAL and does not terminate the process -- matching PyQt6's own default continue-after-slot-exception behavior, just routed through this app's logger. That part is sound. But live-reproducing an exception raised specifically *inside* `paintEvent()` (as opposed to an ordinary slot) surfaced a real, separate problem: `weather_radar_widget.py::paintEvent()` builds its `QPainter` with no try/finally at all, then calls `self._display.draw_radar_elements(...)` -- and `weather_radar_display.py::draw_radar_elements()`'s own `except Exception as e:` block deliberately logs and then `raise`s again rather than swallowing, so a bug anywhere in its large rendering call tree (particle emission, texture compositing, cell/precip/VIL drawing) can reach `paintEvent()` uncaught. Live-reproduced with a minimal widget mirroring this exact shape: an exception raised mid-paintEvent before `painter.end()` is indeed caught by `sys.excepthook` without crashing the process, but the unclosed `QPainter` leaves the widget's Qt backing store corrupted ("QBackingStore::endPaint() called with active painter; did you forget to destroy it or call QPainter::end() on it?") -- and this corruption is *sticky*, reproducing on every subsequent paint of the same widget (not just the one that failed), plus a "QPaintDevice: Cannot destroy paint device that is being painted" error at eventual widget teardown. Root cause: the traceback object retained by `sys.excepthook`'s `tb` parameter (and by Python's own exception-handling machinery) keeps the failed paintEvent's stack frame -- and therefore its local `painter` variable -- alive well past where CPython's normal refcounting would otherwise destroy it and trigger `QPainter`'s RAII-style implicit `end()` on a clean, non-exceptional return; Round 7's earlier assessment that relying on this destructor behavior was safe only considered the normal, non-exceptional return path. Fixed by wrapping `paintEvent()`'s body in the same try/except/finally-with-`painter.isActive()` pattern Round 7 already found "exemplary" in `base_display.py::paintEvent()`, so this widget's own paint entry point now gets the same treatment. Live re-verified with the same minimal reproduction adapted to the fixed shape: the exception is caught locally, `painter.end()` always runs in `finally`, and zero `QBackingStore`/`QPaintDevice` warnings appear on either the failing paint or any subsequent one. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines. |
| `flightManagementSystem.start()`'s FMS update thread was the one exception among ~25 structurally-identical subsystem background-loop threads repo-wide that didn't set `daemon=True` | `Systems/flightManagementSys/flightManagementSystem.py::flightManagementSystem.start()` | **Fixed (August 2026 re-analysis round, Round 13, thread daemon-flag consistency sweep), live-verified.** Swept every `threading.Thread(...)` instantiation repo-wide (over 40 call sites) for daemon-flag consistency. Confirmed ~25 subsystem background-loop threads (ECU, TMS, fuel-management FMS, FDM, PMS, NAV, COMMS, FCC, Airframe, ECS, Hydraulics, GCAS, PerfMonitoring, DefensiveService, SensorService, MissionService, ScenarioEngine, and others) all correctly pass `daemon=True`. Also confirmed `EventBus` and `MessageQueueManager`, whose constructor calls don't pass `daemon=True` inline, still end up correctly daemonized via an explicit `self.thread.daemon = True` attribute assignment immediately after construction -- functionally equivalent, just a different style, initially easy to miss with a naive `daemon=` grep. The one genuine exception found: `flightManagementSystem.start()` created its update thread with neither approach -- `self.thread = threading.Thread(target=self.update)`, no daemon flag anywhere -- meaning it defaulted to *inheriting* the daemon status of whichever thread called `start()`. Since `flightManagementSystem` is boot-critical (started via `system_manager.py::start_async_component()` from the main thread during the async boot sequence), this update thread live-confirmed as non-daemon (`fms.thread.daemon == False`) before this fix. `stop()`'s `self.thread.join(timeout=2.0)` is bounded and, given the loop's fast 20Hz (0.05s) poll interval, reliably completes in normal operation -- no hang was ever observed across this session's ~16 boot+SIGTERM tests -- but this thread was the one place in the whole codebase missing the daemon-flag safety net every sibling subsystem thread has: if `stop()`'s bounded join ever failed to fully complete for any reason (e.g. the loop blocked acquiring `self.lock` inside `update_flight_data()` because some other bug held it), this specific thread -- unlike its ~25 daemonized siblings -- would have kept the whole process alive past a supposedly-complete shutdown. Fixed by adding `daemon=True` to match the established, repo-wide pattern. Live re-verified: `fms.thread.daemon` is now `True` immediately after thread creation, and `stop()` still cleanly joins and clears the thread with no behavior change in the normal case. Separately audited `AsyncMessageHandler.py` (documented, not fixed): it deliberately uses `daemon=False` for all 8 of its threads (workers, periodic-task thread, watchdog, restart threads), with explicit inline comments ("Use non-daemon thread to prevent unexpected termination") showing this was an intentional design choice, not an oversight -- its `stop()` sets the loop's continuation flag to `False` *before* joining (each worker's `while self.running:` loop is bounded by short internal sleeps/timeouts, so this reliably converges well inside the `.join(timeout=2.0)` window in practice), so no fix was applied; changing these to daemon threads would work against the original author's explicit intent to avoid a worker being abruptly killed mid-message-processing. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines. |
| Config file hot-reload / mid-run consistency (checked, not applicable) | `storage/DBM.py`, `core/baseStartUp.py`, `MIL_STD_1553B/Remote_Terminal/RT_messaging/RT_command_map.py`, `MIL_STD_1553B/Remote_Terminal/RT_messaging/message_queue_manager.py`, `Systems/fuelSystems/fuelControl.py`, `Systems/fmFitness/fmStatus.py`, and other XML-config-consuming modules repo-wide | **Checked (August 2026 re-analysis round, Round 14, config file hot-reload sweep), no fix needed -- feature doesn't exist.** Investigated whether any config file (`dbConfig.xml`, `command_registry.xml`, `address_book.xml`, etc.) is ever re-read after boot, which would raise consistency questions (partial-write visibility, stale cached values, concurrent-read-during-write). Found no such mechanism anywhere: every `ET.parse(...)` call site checked is either invoked exactly once at module-import time into a module-level constant (e.g. `RT_command_map.py`'s `COMMAND_REGISTRY = _load_command_registry()`, `SYSTEM_MAP, SUBSYSTEM_MAP = _load_address_book()`, evaluated once when the module is first imported) or once inside a class's `__init__`/boot-path method, never re-invoked afterward. This codebase has no hot-reload/mid-run config-refresh feature at all -- config is exclusively boot-time, read-once, then held as an in-memory value for the process's entire lifetime -- so there's no reachable partial-update or stale-read scenario to fix. |
| `RadarDisplayDataCoordinator._data_store` (backing `store_data()`/`cleanup_expired()`/`reset_data()`) had no locking at all -- a new radar data type's first-ever `store_data()` call (dict key insertion) could race with a concurrent `cleanup_expired()`/`reset_data()` iterating the same dict | `Interfaces/userInterface/displays/radar/radar_display_data_coordinator.py::RadarDisplayDataCoordinator` | **Fixed (August 2026 re-analysis round, Round 14, radar data TTL/expiration correctness sweep), live-verified.** Investigated the TTL/expiration logic backing weather-radar precipitation/VIL/cell/targeting/SAR/TFR/AEWC/windshear/turbulence data. The TTL comparison itself (`item_age = current_time - self._timestamps[item_id]; if item_age < ttl: valid_items.append(item)`) is correct -- no off-by-one, strict-less-than is the right boundary. But `cleanup_expired()` (called every single `paintEvent()` via `draw_radar_elements()`, always on the Qt GUI thread) iterates `for data_type, store in self._data_store.items():` with zero locking, while `store_data()` -- reachable from `radar_to_display_bridge.py`/`display_message_router.py`, both driven by `AsyncMessageHandler`'s worker-thread message dispatch, i.e. a genuinely different OS thread -- inserts a brand-new key into that same dict (`self._data_store[data_type] = {...}`) the first time any given radar type's data arrives; `__init__` only pre-populates 3 of the 9+ live data types (`precipitation`/`vil`/`cells`), so `targeting`/`sar`/`tfr`/`aewc`/`windshear`/`turbulence` each trigger this exact insertion path on their first message, which could arrive at any point during a live session. Live-reproduced with a 2-thread stress test (one thread calling `store_data()` with novel data types, one calling `cleanup_expired()` in a loop, GIL switch interval tightened to widen the race window): `RuntimeError: dictionary changed size during iteration` on the first trial. `reset_data()` (reachable from the GUI thread via `WeatherRadarWidget`'s reset path) has the identical `for type_name in self._data_store.keys():` pattern and the same live risk. Also found a narrower, non-crashing lost-update race: `store_data()`'s `self._data_store[data_type]['current'] = processed_items` and `cleanup_expired()`'s `store['current'] = valid_items` are both full reassignments (not in-place mutation) of the same dict value -- without synchronization, whichever write lands second silently clobbers the other, which for the `store_data()`-then-`cleanup_expired()` ordering means a just-stored batch of fresh radar data could be silently overwritten by a stale, already-filtered copy computed from data read *before* the fresh write landed. Fixed by adding a `threading.RLock()` (reentrant, not a plain `Lock` -- `store_data()` itself periodically calls `self.cleanup_expired()` on the same thread, which would deadlock a non-reentrant lock) and using it to guard: the get-or-create block in `store_data()`, the `current`/`backup` reassignment in `store_data()`, the full body of `cleanup_expired()`, and the full body of `reset_data()`. Live re-verified with an escalated 3-way concurrent stress test (`store_data()`, `cleanup_expired()`, and `reset_data()` all hammering the same coordinator simultaneously across 30 trials): zero errors, versus the original single-trial reproduction of the crash before the fix. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines. |
| `AsyncMessageHandler._run_periodic_tasks()` iterated `self.systems.values()` with no lock, while `register_system()` inserts new keys into the same dict under `self.lock` whenever a not-yet-seen system name is first encountered | `local_messaging/routing/handlers/sync_handler/AsyncMessageHandler.py::AsyncMessageHandler._run_periodic_tasks()` / `register_system()` | **Fixed (August 2026 re-analysis round, Round 15, message routing table staleness sweep), live-verified.** Investigated message routing tables for staleness/duplicate-registration bugs and for the same dict/set mutation-during-iteration bug class found and fixed twice already this session (Round 12's `DisplayNode`, Round 14's `RadarDisplayDataCoordinator`). `SystemHandler.register_handler()` and `AsyncMessageHandler.register_system()` both already handle duplicate registration correctly and idempotently (silent skip / reuse-existing-instance, both lock-guarded) -- no bug there. But `register_system()` is called reactively, not just at boot -- `RadarMessageHandler`/`FMSMessageHandler`/`DisplayMessageHandler`/`FCSMessageHandler` all lazily call it the first time they're actually used, which can happen at any point during a live session -- and it inserts new keys into `self.systems` under `self.lock`. Meanwhile `_run_periodic_tasks()` (running continuously on its own background thread from early in the app's life) iterated `for system in self.systems.values():` with no locking at all. Live-reproduced with a construct mirroring both methods' exact locking/iteration shape (GIL switch interval tightened to widen the race window): a concurrent `register_system()`-style insert while the unlocked iteration was in progress raised `RuntimeError: dictionary changed size during iteration` on the first trial. (The two other `self.systems` iteration sites, both inside health-check methods, were already correctly wrapped in `with self.lock:` -- only `_run_periodic_tasks()` had the gap.) Fixed by taking a lock-protected snapshot (`with self.lock: systems_snapshot = list(self.systems.values())`) before iterating, rather than holding the lock for the entire per-cycle task-processing duration (which could otherwise add unnecessary contention against other threads calling `register_system()`) -- the same snapshot-iteration pattern already proven correct for the `DisplayNode` fix in Round 12. Live re-verified with the same reproduction adapted to the fixed shape: 0 errors across 20 trials. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines. |
| Integer-vs-float division / numeric truncation bugs (checked, not applicable to this codebase) | whole repo | **Checked (August 2026 re-analysis round, Round 15, numeric division sweep).** Swept for the classic integer-division-where-float-was-intended bug class, distinct from Round 9's numpy-dtype-overflow sweep -- this angle is about the Python `//` operator and `int()` truncation, not numpy casting. Found zero uses of `//` (floor division) anywhere in the codebase outside `Tests/`. Since this is a Python 3 codebase (confirmed throughout the session via pervasive f-string usage, `asyncio`/`qasync`, etc.), the plain `/` operator always performs true (float) division regardless of operand types -- the classic Python-2-era int-division gotcha this angle was checking for doesn't exist in Python 3 by construction of the language itself, and this codebase never opts back into floor division via `//`. Separately swept `int(...)` truncation call sites in physics/nav/attitude code for silent precision loss: every one found either converts a config-file string to an ID/integer field (correct, no precision to lose) or deliberately packs a float into a scaled fixed-point integer for MIL-STD-1553B bus-word transmission (e.g. `int(self.attitude['roll'] * 100) & 0xFFFF`, standard scaled-integer avionics bus encoding) -- the internal float state these values are derived from is never itself replaced by the truncated version, so no precision is lost for any subsequent internal calculation, only for the transmitted/logged representation. No fix needed. |
| Repo-wide sweep for other unlocked shared dict/list iteration bugs (checked, low incremental risk found in the pattern already fixed 3x this session) | `Interfaces/userInterface/displays/radar/radar_display_factory.py`, `storage/DBM.py`, `local_messaging/routing/response_services/system_response_services/DisplayResponseService.py` | **Checked (August 2026 re-analysis round, Round 16), no fix needed.** Followed up on the same dict/set mutation-during-iteration bug class found and fixed three times already this session (Round 12's `DisplayNode`, Round 14's `RadarDisplayDataCoordinator`, Round 15's `AsyncMessageHandler.systems`) to check for further unaudited instances. `RadarDisplayFactory._display_cache` has no lock at all, but its one full-dict iteration site already defensively wraps it in `list(cls._display_cache.items())` -- someone already knew about this exact hazard for this specific class -- and every other access is a widget-construction operation, which by Qt's own hard single-thread requirement can only ever happen on the GUI thread anyway, so there's no genuine cross-thread caller to race against in practice. `DBM.py`'s `self.systems` dict is iterated once in `shutdown()` (end of process lifetime, after all systems are already registered) and populated exclusively inside the one-time `initialize_system_databases()` boot method (iterating the static `dbConfig.xml`-derived config, not reactively per-message like the `AsyncMessageHandler.systems`/`RadarDisplayDataCoordinator` cases were) -- no reachable window for the race. `DisplayResponseService.pending_requests`/`_processing_locks` are only ever accessed by unique per-request-ID point lookups (`in`/`[key]`/`.pop(key)`), never iterated as a whole, so the specific `RuntimeError`-during-iteration failure mode doesn't apply regardless of locking. No further live instances of this bug class found this round. |
| `MissionPlanningSystem.get_next_waypoint()`/`optimize_route()`/`assess_threats()` read/mutated `self.waypoints`/`self.threats` with no locking, while `add_waypoint()`/threat-adding methods mutate the same structures under `self.lock` | `Systems/missionPlanning/missionControl.py::MissionPlanningSystem` | **Fixed (August 2026 re-analysis round, Round 16, scenario engine / mission planning state-machine race sweep), live-verified.** `Interfaces/scenarios/scenarioEngine.py` checked out clean -- every access to its `self._events` list (mutated in `load()`, read/mutated in `_update_loop()`/`start()`/`stop()`) is already consistently wrapped in `with self._lock:`. `MissionPlanningSystem` (confirmed live via `missionService.py`, which instantiates it and drives it from its daemon `MISSION_Update` thread via `MissionControl.tick()`) had inconsistent locking instead: `add_waypoint()`, `update_current_position()`, `update_target_status()`, and the threat-adding method all correctly acquire `self.lock`, but `get_next_waypoint()` read `self.waypoints` with no lock at all, and `optimize_route()`/`assess_threats()` mutated/iterated `self.waypoints`/`self.threats` with no lock either -- despite `get_next_waypoint()`/`optimize_route()` being reachable from the daemon `MISSION_Update` thread (via `MissionControl.tick()`, called every cycle) while `add_waypoint()` is reachable from a different thread in practice (an `AsyncMessageHandler` worker thread processing an incoming add-waypoint message). Live-reproduced with the real class: a 2-thread stress test (one thread calling `add_waypoint()`, one calling `optimize_route()`) raised `ValueError: list modified during sort` on the first trial -- `list.sort()` detects and rejects concurrent mutation of the list it's sorting, a different but analogous failure mode to the dict-changed-size-during-iteration crashes found earlier this session. Fixed by changing `self.lock` from a plain `threading.Lock` to a `threading.RLock` (reentrant, since `optimize_route()` calls `self.assess_threats()` internally on the same thread, which would deadlock a non-reentrant lock once `assess_threats()` itself became lock-guarded) and wrapping `get_next_waypoint()`, `assess_threats()`, and `optimize_route()`'s full bodies in `with self.lock:`. Live re-verified with an escalated 3-way concurrent stress test (`add_waypoint()`, `optimize_route()`, and `get_next_waypoint()` all hammering the same instance simultaneously across 30 trials): zero errors, versus the original single-trial reproduction of the crash before the fix. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines. |
| `logging.handlers.RotatingFileHandler`/`FileHandler` calls across `Utils/logger/sys_logger.py` and `Utils/logger/test_log_handler.py` had no explicit `encoding=` argument, defaulting to the platform's locale-preferred encoding rather than UTF-8 | `Utils/logger/sys_logger.py`, `Utils/logger/test_log_handler.py` | **Fixed (August 2026 re-analysis round, Round 17, encoding/non-ASCII sweep), live-verified.** Investigated non-ASCII character handling in messages and logs. The MIL-STD-1553B word decoders' `.decode('ascii')` calls (`RT_words_decoder.py`, `BC_words_decoder.py`) are correct as-is -- they only ever decode strings of '0'/'1' binary-digit characters, which are always within ASCII range by construction, not free text. The real finding: ~20 files repo-wide already contain literal non-ASCII characters directly in their Python source (mostly Unicode arrows/em-dashes used in comments and, critically, inside live `logger.X(f"...")` calls -- e.g. `missionControl.py`'s `logger.info(f"[MPS] Target {target_id} \u2192 {status}")`, executed on every target status change), while every log file this app writes (`RotatingFileHandler` in `sys_logger.py`; three separate `FileHandler`/`open()` calls in `test_log_handler.py`) opened with no explicit `encoding=` argument at all. Python's `logging.FileHandler` falls back to the platform's locale-preferred encoding when none is given, which on Windows (a supported deployment target -- `install.py` branches on `platform.system()`) is typically a restrictive single-byte codepage like `cp1252`, not UTF-8, for the Python versions this project targets (3.10-3.14, predating PEP 686's UTF-8-by-default on Windows). Live-reproduced by opening a `RotatingFileHandler` with `encoding='cp1252'` (simulating the Windows default) and logging the exact live line quoted above: `UnicodeEncodeError` is raised inside `logging`'s own `emit()`, caught internally by `handleError()` so it doesn't crash the caller, but the log line is silently dropped entirely (0 bytes written) and a "--- Logging error ---" traceback prints to stderr *every single time that line executes* -- a real, repeating loss of exactly the log data this entire session's verification methodology has depended on, on any Windows deployment. Fixed by adding `encoding='utf-8'` to all four file-opening call sites (the main `RotatingFileHandler`, the per-test and startup `FileHandler`s, and the startup handler's own log-copying `open()` read) -- UTF-8 can represent every character these log messages use, and is a no-op behavior change on Linux/macOS where UTF-8 is already the typical default. Live re-verified: the same reproduction with `encoding='utf-8'` instead of `'cp1252'` logs the line intact with the arrow character preserved, zero exceptions. Verified: ruff clean, full 173-test suite passes, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines, 0 "Logging error" occurrences anywhere in the boot log. |
| `QThread` usage / Qt object background-thread cleanup patterns (checked, not applicable) | `Interfaces/userInterface/managers/display_manager.py` | **Checked (August 2026 re-analysis round, Round 17, QThread/Qt-object-lifecycle sweep), no fix needed.** `QThread` is referenced in exactly one file, and only ever via `QThread.currentThread()` for main-thread-identity assertions (`if QThread.currentThread() is not QApplication.instance().thread(): raise RuntimeError(...)`, 9 occurrences) -- it is never subclassed or instantiated as an actual worker thread anywhere in this codebase, which uses plain `threading.Thread` exclusively for background work (already exhaustively audited for daemon-flag consistency in Round 13). Since there's no QThread-based worker pattern in use, there's no QThread object lifecycle, `moveToThread()`, or cross-thread QObject-ownership concern to audit -- the premise of this angle doesn't apply to this codebase's architecture. The main-thread-identity guards that do exist are correctly implemented (fail loudly via `raise RuntimeError`, not a silent proceed-anyway), consistent with this session's broader finding (Round 11) that this app takes Qt cross-thread safety seriously. |
| JSON/dict serialization round-trip fidelity for message payloads (checked, not applicable) | Repo-wide grep for `json.dumps`/`json.loads`/Enum/`datetime` usage in message-passing paths | **Checked (August 2026 re-analysis round, Round 18, JSON round-trip sweep), no fix needed.** Investigated whether Enum members or `datetime` objects ever get handed to `json.dumps()` un-converted (both raise `TypeError: Object of type X is not JSON serializable` if so). All live `datetime.now()` usage found in message payloads (`fms_navigation_data.py`, `fms_tactical_data.py`, `fms_velocity_data.py`, `fms_attitude_data.py`) consistently calls `.timestamp()` before storing/sending, producing a plain float -- clean. Found one near-miss while checking Enum-typed fields: `MissionControl.get_snapshot()` (`missionControl.py`) returns `'phase': self.phase`, which for one moment looked like it needed `.value` to match `MissionPlanningSystem.phase`'s `MissionPhase` Enum elsewhere in the same file. Live-verified this was wrong before committing to it -- `mc = MissionControl(); mc.get_snapshot()` raised `AttributeError: 'str' object has no attribute 'value'` the moment `.value` was added, revealing `MissionControl.phase` is actually a separate `@property` on a different class that always returns a plain string (`self._phase`), never an Enum. Reverted immediately and added a comment at both `phase` definitions in `missionControl.py` distinguishing the two so a future pass doesn't make the same mistake. No functional change; `json.dumps(MissionControl().get_snapshot())` confirmed clean both before and after. |
| Display factory / widget cache thread-safety under Qt reparenting (checked, not applicable) | `Interfaces/userInterface/displays/*/mfd_display_factory.py`, `pfd_display_factory.py`, `holographic_settings_panel.py`, `mfd_container.py` | **Checked (August 2026 re-analysis round, Round 18, display-factory thread-safety sweep), no fix needed.** Audited every call site of the display factories' `create_display()`/widget-cache methods for cross-thread access, given this session's repeated pattern (Rounds 12, 14, 15, 16) of unlocked shared containers touched from more than one thread. Exhaustive caller trace confirms all of them -- `mfd.py`'s mode-switch handler, `holographic_settings_panel.py`'s theme-change callback, `mfd_container.py`'s tab/reparent logic -- are Qt slots invoked exclusively on the GUI thread (button clicks, `QTimer` callbacks, signal/slot deliveries), never from a background `threading.Thread`. No lock is needed because there's no concurrent access to guard against in practice; this validates the same GUI-thread-only assumption Round 16's `MissionPlanningSystem` fix relied on for its own reasoning about what *does* need a lock. |
| `FuelSystem.add_tank()`/`add_engine()` used a plain `INSERT` against a `TEXT PRIMARY KEY`-constrained table in the persistent, non-ephemeral `default.db`, which every process boot re-populates with the same fixed tank/engine names | `Systems/engineManagement/fuelManagement/fuelControl.py` | **Fixed (August 2026 re-analysis round, Round 18, synthesis-pass test run), live-verified.** Found by accident while running this round's mandatory full CI-equivalent suite: `test_power_fuel_thrust.py` still reported 43/43 assertions passed (the in-memory `self.tanks`/`self.engines` dicts are populated before the DB call and don't depend on it succeeding), but its log tail was flooded with `sqlite3.IntegrityError: UNIQUE constraint failed: fuel_tanks.name` / `engines.name`, each retried 3x by `execute_query_async()` then only logged, never raised to the caller. Root cause: `get_fuel_management_system()` is called unconditionally from `system_manager.py`'s live boot sequence, and on first construction runs `setup_aircraft()`, which calls `add_tank("Main Left", ...)`/`add_tank("Main Right", ...)`/`add_tank("Center", ...)` and `add_engine("Engine 1", ...)`/`add_engine("Engine 2", ...)` with the same fixed names every time. `_setup_database()` creates `fuel_tanks`/`engines` with `CREATE TABLE IF NOT EXISTS` against `FMOFP/storage/databases/default.db`, an on-disk file that (unlike the in-memory `_fms_instance` singleton) survives process restarts -- so a plain `INSERT` against `name TEXT PRIMARY KEY` was guaranteed to collide with the previous boot's rows on every restart after the very first, silently failing to persist that tank/engine's current capacity/level/thrust to disk while spamming the log with retry/rollback ERROR lines (260+ possible per boot, the same log-noise class as the fixed missing-table gap documented above in section 14). Live-reproduced directly: constructing three successive `FuelManagementSystem()` instances against the same `default.db` (simulating three process restarts) raised the exact `IntegrityError` on the 2nd and 3rd every time before the fix. Fixed by switching `add_tank()`/`add_engine()` from `insert_into_table()`'s plain `INSERT` to a hand-built `INSERT OR REPLACE` query (matching the existing precedent for this exact upsert-on-restart pattern already used in `Utils/common/paths.py` and `DisplayResponseService.py`), making both calls idempotent across restarts. Re-verified live: the same three-successive-construction test now produces zero errors on all three boots, `tanks`/`engines` dict contents identical each time. Also corrected a stale, backwards comment in the same class's `__init__` that claimed `FuelSystem` was dead code superseded by `FuelSystemController` -- a repo-wide grep confirms the opposite: `FuelSystemController` is never imported anywhere outside its own file (that one is the actual dead code, already documented as such in section 14's "Implemented-but-Unwired Subsystems" table), while `FuelSystem` (via `FuelManagementSystem`) is constructed every boot by the live `system_manager.py` startup sequence. Considered but deliberately left as-is: `connect_tank_to_engine()`'s `fuel_lines` table has an `AUTOINCREMENT` primary key with no uniqueness constraint on `(tank_name, engine_name)`, so it accumulates duplicate rows across restarts rather than erroring -- lower severity (no crash, no log spam) and confirmed via repo-wide grep that nothing ever reads `fuel_lines` back from the database (the in-memory `self.fuel_lines` dict, rebuilt fresh by `setup_aircraft()` every boot, is the only thing any caller -- e.g. `fuelMonitor.py` -- actually consults), so the unbounded row growth has no functional impact; not worth the added complexity of a schema migration for a table nothing reads. Verified: ruff clean, full 233-assertion/7-file CI-equivalent suite passes with zero IntegrityErrors or tracebacks anywhere in the output, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot and clean SIGTERM-triggered exit with 0 real ERROR/CRITICAL/traceback lines in the boot log. |
| `time.time()` (wall clock) used to measure elapsed/timeout duration across ~13 call sites; vulnerable to backward clock jumps (NTP/GPS correction, manual adjustment, VM live migration) | `RT.py` (fixed); also present in `DBM.py`, `RT_socket.py`, `BC_socket.py`, `event_driven_communication.py`, `fmsMessenger.py`, `RadarResponseService.py`, `weather_radar_display.py` (documented, not fixed) | **Fixed at the highest-impact call site; documented as a known architectural pattern elsewhere (August 2026 re-analysis round, Round 19, wall-clock-vs-monotonic-clock sweep).** Investigated the alternate framing of "timestamp consistency" first -- datetime.now()/utcnow() mixing, timezone-naive vs aware comparisons -- and found nothing: every message-payload timestamp in the codebase already goes through `datetime.now().timestamp()` (confirmed clean in Round 18), and DBM's rate-limiter is the only other `datetime.now()` arithmetic, entirely self-consistent within one process. Pivoted to a related but distinct angle: nearly every timeout/elapsed-duration check repo-wide is written as `time.time() - start_time > timeout` rather than using `time.monotonic()`, which Python's own docs recommend specifically for this purpose because, unlike `time.time()`, it's guaranteed non-decreasing and immune to wall-clock adjustments. Live-reproduced the failure mode on the clearest, highest-impact instance: `RT.py`'s `get_message(wait=True, timeout=2)` polls in a `while True` loop checking `(time.time() - start_time) > timeout`. Monkeypatching `time.time()` to simulate a 600-second backward jump immediately after `start_time` was captured (as a real NTP/GPS correction would look from inside the process) made the call hang past a 15-second outer deadline instead of returning `None` after 2s as requested -- because `time.time() - start_time` stays negative forever under a sustained backward-shifted clock, which is never `> timeout`, so the "Timeout exceeded" branch can never be reached. **Fixed** by switching `get_message()`'s two `time.time()` calls to `time.monotonic()`. Re-verified live with the identical reproduction (wall clock still jumping backward via the same monkeypatch, `time.monotonic()` left untouched, matching how a real OS-level wall-clock adjustment behaves): the call now correctly returns `None` after 2.01s of real elapsed time. **Deliberately left unfixed elsewhere**: grepped the full tree for the same `time.time() - X {>,<}` pattern and found ~12 more call sites (`DBM.py`'s two 10s SQL timeouts, `RT_socket.py`/`BC_socket.py`'s acknowledgment/health-check windows, `event_driven_communication.py`'s event-timeout check, `fmsMessenger.py`'s queue-manager startup wait, `RadarResponseService.py`'s poll loop, `weather_radar_display.py`'s mode-transition debounce, plus the `RT_transfer_aggregator.py`/`BC_transfer_aggregator.py` pair which store *absolute* deadlines as `time.time() + TRANSFER_TIMEOUT` rather than a relative elapsed check, a materially different and riskier pattern to convert). Converting all of these is a genuine, behavior-affecting refactor spanning many independent, already-verified subsystems -- a fundamentally different scope than this session's established narrow-fix precedent (e.g. Round 17's encoding fix, a contained 4-line no-op-on-Linux change) -- and none of the others were confirmed to have `get_message()`'s specific "caller blocks forever" consequence: the socket health-check and acknowledgment-window sites degrade to an incorrect boolean (fail-safe, not a hang), and the poll/debounce sites bound their own outer loops independently. Recorded here as a known architectural limitation for a future dedicated pass, not attempted piecemeal in this round given the risk of an incomplete, inconsistently-mixed monotonic/wall-clock refactor across files this round didn't have time to individually re-verify. |
| Float equality (`==`/`!=`) comparisons in physics/threshold-checking code (checked, not applicable) | `thrustManagement.py`, `fuelControl.py`/`fuelMonitor.py`, `globalPositionSystem.py`, weather radar coordinate generators | **Checked (August 2026 re-analysis round, Round 19, float-epsilon sweep), no fix needed.** Searched for direct `==`/`!=` comparisons between two independently-computed floats (the class of bug where floating-point representation error makes a threshold never trigger, or a convergence check never settle) across every threshold/limit check reachable from the physics and engine-management systems. Found none: `ThrustManagementSystem.check_asymmetry()`'s safety limit, the autothrottle PI loop, and `FuelMonitor`'s low/critical-fuel alerts all correctly use ordering comparisons (`>`, `<`), which are inherently epsilon-safe regardless of float representation error. The `== 0`/`== 0.0` comparisons found elsewhere (`globalPositionSystem.py`'s `r_mag`/`d_mag`/`rng_est` zero-magnitude guards, the weather-radar coordinate generators' zero-coordinate filtering) are all guarding against an exact, intentional zero (the origin point, or a `range == 0` sentinel from upstream integer/index data), not comparing two independently-derived floating-point results that could differ by representation error alone -- not the vulnerable pattern this sweep was looking for. |
| Mutable default arguments (`def f(x=[])`/`def f(x={})`) (checked, not applicable) | Whole `FMOFP` tree (excluding `Tests`) | **Checked (August 2026 re-analysis round, Round 20, mutable-default-argument sweep), no fix needed.** Parsed every `.py` file under `FMOFP` (excluding `Tests`) with Python's own `ast` module and walked every `FunctionDef`/`AsyncFunctionDef` node's positional and keyword-only default values looking for an `ast.List`/`ast.Dict`/`ast.Set` literal -- the classic gotcha where a mutable default is created once at function-definition time and silently shared/mutated across every call that doesn't pass its own value. Zero matches across the entire codebase; every mutable-seeming default already uses the `None`-sentinel-plus-`if x is None: x = []` pattern or takes no default at all. A clean, AST-verified negative result, not just a regex spot-check. |
| Four live, wired-in subsystem singletons' background-thread `run()` loops (`HydraulicSystemController`, `AirframeSystemManager`, `ECSControl`, `FlightManagementFitness`) had no exception handling at all, so any uncaught exception permanently and silently killed that subsystem's monitoring thread with no restart | `Systems/hydraulics/hydrControl.py`, `Systems/airframeSystemManagement/airframeControl.py`, `Systems/enviornmentalControlSystem/ecsControl.py`, `Systems/fmFitness/fmStatus.py` | **Fixed (August 2026 re-analysis round, Round 20, background-thread crash-resilience sweep), live-verified.** This session has repeatedly found and fixed the self-healing try/except-per-iteration pattern missing or broken in specific spots (Round 12's `stop()` TOCTOU work, the interruptible-backoff fix already documented inline in `ThrustManagementSystem`/`MissionService`), but never swept every `threading.Thread(target=...)` loop repo-wide for whether it has *any* exception handling at all. AST-parsed all ~25 thread-target functions in the codebase for a `while` loop with no `Try` node anywhere in the function body, cross-referenced against PLANNING.md section 14's "Implemented-but-Unwired Subsystems" table to filter out confirmed-dead code (the check correctly flagged `MissionManagementSystem.update()` too, but that class -- unlike the four below -- is confirmed unreferenced anywhere outside its own file, i.e. genuinely dead code per this session's existing documentation, so left as-is per this session's standing rule). Four matches were live, wired-into-`system_manager.py`-boot subsystems: `HydraulicSystemController.run()`, `AirframeSystemManager.run()`, `ECSControl.run()`, `FlightManagementFitness.run()`. Live-reproduced on `HydraulicSystemController`: deleted the `"critical"` key from `self.pressure_thresholds` (simulating any real-world fault -- bad config, transient sensor read failure -- that would raise inside the loop body) and called `start()`. The very first tick's `KeyError` propagated all the way out of `run()`, killing the daemon thread permanently -- `thread.is_alive()` was `False` after the first tick and stayed `False` 3 seconds later with no recovery attempt. The failure was visible only as a raw Python traceback printed to stderr via `threading.excepthook`, never logged through this app's own `logger`/log file -- in a production deployment where stderr isn't captured back into the app's log, this would be a completely silent, permanent loss of an entire flight-critical subsystem's monitoring loop. Fixed by wrapping each loop's per-tick body in `try`/`except Exception as e: logger.error(...)`, matching the self-healing pattern already established in `ThrustManagementSystem`/`MissionService`/`NavService`'s own update loops (log-and-continue at the existing tick cadence, no special backoff needed since these loops don't hold any lock or resource across ticks). Re-verified live: the identical `HydraulicSystemController` reproduction now logs `"[HYDRAULICS] Monitor error: 'critical'"` on every tick and stays alive through at least 3 consecutive failures, with `stop()` still completing cleanly afterward. Also force-verified the other three the same way (monkeypatching each subsystem's monitor method to always raise `RuntimeError('forced')`): all three logged the error every tick and stayed alive across multiple consecutive failures. Verified: ruff clean, full 233-assertion/7-file CI-equivalent suite passes with zero new tracebacks, boot+SIGTERM test (polled every 2s for 20s) shows clean stable boot (including all four fixed subsystems' normal startup log lines) and clean SIGTERM-triggered exit, 0 real ERROR/CRITICAL/traceback lines in the boot log. |


### 🚧 Stub / Incomplete Subsystems

As of the August 2026 audit, Defensive Systems, Sensor Management, Mission Planning, and
Avionics Hardware Health are no longer believed to be stubs (see "Fully Operational"
above) — this table previously listed them as such based on a stale claim that was never
independently verified against the actual code. Nothing has been identified so far as a
genuine stub within the 4 subsystems audited. Known remaining gaps, found as side effects
of the audit rather than from a targeted search, so this list should not be treated as
exhaustive:

| System | Location | Status |
|--------|----------|--------|
| ~~Fuel Systems config loading~~ | ~~`Systems/fuelSystems/fuelControl.py`~~ | **Fixed** — confirmed live (August 2026 audit) that `fuelControl.py` already resolves `fuelConfig.xml` via `os.path.dirname(os.path.abspath(__file__))`, not a fragile CWD-relative path; this table entry was stale |
| ~~Wind shear + turbulence display~~ | ~~`weather_radar_display.py`~~ | **Fixed** — `_draw_windshear_overlay()` and `_draw_turbulence_overlay()` added; dedicated bridge functions `push_windshear_data()` / `push_turbulence_data()` added |
| ~~Three divergent radar mode enum definitions~~ | ~~`Systems/radarManagement/radar_enums.py`, `Interfaces/predefinedMessages/radar_enums.py`, `Interfaces/userInterface/displays/radar/radar_enums.py`~~ | **Fixed (August 2026).** `Systems/radarManagement/radar_enums.py` is now the single canonical source for all 6 radar mode enums (`RadarMode`, `weather_radarMode`, `targeting_radarMode`, `sar_radarMode`, `aewc_radarMode`, `tfr_radarMode`); the other two files now import and re-export from it instead of redefining their own classes with different values. Verified live that `TargetingRadarMessages.set_targeting_radar_mode(targeting_radarMode.SEARCH)` — which previously sent wire value 2 (the old `predefinedMessages` numbering) that the real radar would have decoded as `DEGRADED` under the canonical 2 — now sends 40, matching what `targeting_radar.py` itself validates against. Added `TRACKING` as an additional alias (same value as `TRACK`) to the canonical `targeting_radarMode`/`aewc_radarMode` since that name only existed in the old `predefinedMessages` definition and several call sites reference it. Full CI-equivalent suite and a full `SystemStart.py` boot both pass cleanly (zero errors/tracebacks in the boot log) after the change. The residual dual-sys.path identity issue flagged here has since been fixed separately — see `Utils/dual_path_compat.py` below. |
| ~~Dual sys.path import identity split~~ | ~~`Utils/dual_path_compat.py` (new), hooked in via `Utils/common/fetching.py` and `SystemStart.py`~~ | **Fixed (August 2026).** The codebase adds both `B20SS/` and `B20SS/FMOFP/` to `sys.path` everywhere, so the same physical file loaded via a bare import (`import Systems.X`) versus an `FMOFP.`-prefixed absolute import (`import FMOFP.Systems.X`) was silently becoming two separate module/class objects — confirmed live to also affect the `SysLogger` singleton (`get_logger()`) and `RadarDataFusion`'s `__new__`-based singleton pattern, not just the radar enums. Fixed with a `sys.meta_path` finder that redirects any bare import of `Systems`, `Utils`, `Interfaces`, `core`, or `storage` (the packages confirmed to actually be bare-imported anywhere in this codebase — 103 files for `Utils` alone) to resolve to the already-loaded `FMOFP.<name>` module, recursively for every submodule depth, without needing to enumerate them in advance. Installed automatically via `Utils/common/fetching.py`'s `fetch()` (already transitively imported by ~40 files including the logger, so it reaches nearly every code path with no per-entry-point wiring needed) plus a redundant direct call in `SystemStart.py` for extra safety. Verified live: `Systems.X is FMOFP.Systems.X` for top-level packages, deep submodules (`targeting_radar.py`), `from X import Y` style imports, the logger singleton, and the `RadarDataFusion` singleton — all now return identical objects regardless of import spelling, in both import orders (bare-first and absolute-first). Full CI-equivalent suite and a full `SystemStart.py` boot both pass cleanly after the change, with no measurable startup slowdown. |

### 🔌 Implemented-but-Unwired Subsystems

Found via a broad live-exercise pass across the entire `Systems/` tree (August 2026,
"is there anything else to address" follow-up round). Distinct from the "stub" claims
above: these are not incomplete implementations — most construct and run their core
methods correctly once tested directly — they are simply never instantiated by
`system_manager.py` or anywhere else, so they never run as part of the live simulation
today, unlike e.g. Defensive Systems / Sensor Management / Mission Planning / Avionics
Hardware Health (which *are* wired in). Confirmed via `grep` for each class name across
the whole codebase outside its own directory.

| System | Location | Status |
|--------|----------|--------|
| ~~`PowerManagementSystem`~~ | `Systems/powerManagement/elec/powerManagementSystem.py` | **Wired into the live boot sequence (August 2026, this round).** Previously fixed but still unwired (`_setup_database()` positional-arg bug, commit `c08d686`); now started from `system_manager.py` Phase 3 via a new `get_power_management_system()` singleton. Also substantially enhanced while wiring it in: the old `main_battery_charge`/`aux_battery_charge` bare floats were replaced with the real `MainBattery`/`EmergencyBattery`/`APUBattery`/`MissionEquipmentBatteryBank` objects (built earlier this round, see the now-superseded row below), plus a `CoolingSystem`/`HVACSystem` pair updated every tick. Its own `receive_message()` still references a `self.message_handler` that's commented out in `__init__` — genuinely incomplete, not called by anything, left as-is. See commit `2603419`. |
| `ElectricalPowerController` | `Systems/electricalPower/powerControl.py` | Never instantiated anywhere. Two bugs (config path, `supplyOrder` int() crash) already fixed in an earlier pass this session — see Known Issues comment inline in the file. Re-verified live (August 2026): construction and `monitor_generators()`/`manage_distribution()` both clean. **Still not wired in as of this round** — `PowerManagementSystem` above was chosen as the live power service instead (it, not this class, is what the new battery/cooling/HVAC classes were built against), so this remains a documented-but-unused duplicate, same pattern as `FuelSystemController` below. |
| `FuelSystemController` | `Systems/fuelSystems/fuelControl.py` | Never instantiated anywhere. Config path already fixed (see Known Issues above). Re-verified live (August 2026): `run()` (`monitor_fuel_levels`/`perform_refuel`/`control_feed_lines`) clean. **Correction (August 2026, this round): no longer the "primary" fuel implementation** — that designation was given to this class earlier in the audit before `FuelManagementSystem` below existed in wired-in, tested form. `FuelManagementSystem`/`FuelSystem` is now the live fuel system; this class is unwired and superseded by it. |
| ~~`FuelManagementSystem` / `FuelSystem`~~ | `Systems/engineManagement/fuelManagement/fuelControl.py` | **Now the primary, wired-in fuel implementation (August 2026, this round)** — promoted over `FuelSystemController` above because it's the one `FuelMonitor`/`FuelTransferManager` (see the now-superseded row below) were purpose-built against (`tanks`/`get_fuel_status()`/`transfer_fuel()`/`fuel_lines`/`engines`), and because it tracks real per-tank state instead of `FuelSystemController`'s hardcoded simulated values. Previously fixed but unwired (`get_system_db('engine')` ValueError, undefined-`self.scheduler` AttributeError, commit `c08d686`); now started from `system_manager.py` Phase 3 via a new `get_fuel_management_system()` singleton, with `FuelMonitor.poll()`/`FuelTransferManager.update()` driven every tick from inside `run()`. **Second bug found and fixed while wiring this in:** `FuelSystem.__init__()` never created the `fuel_tanks`/`engines`/`fuel_lines` tables that `add_tank()`/`add_engine()`/`transfer_fuel()`/`update()` all write to — confirmed live via a full boot+SIGTERM test producing 260 "no such table: fuel_tanks" ERROR lines in 15s before the fix, 0 after. See commit `2603419`. |
| ~~`HydraulicSystemController`~~ | `Systems/hydraulics/hydrControl.py` | **Wired into the live boot sequence (August 2026).** Previously fixed but unwired (busy-loop `run()`, commit `50e0686`); now started from `system_manager.py` Phase 3 via a new `get_hydraulic_system_controller()` singleton (`start()`/`stop()` gate the existing monitor loop with a `threading.Event`). See commit `18cba09`. |
| ~~`ECSControl`~~ | `Systems/enviornmentalControlSystem/ecsControl.py` | **Wired into the live boot sequence (August 2026).** No config file, readings still hardcoded (a genuine stub, not fixed — no real sensor data source exists to wire up). **Bug found and fixed while wiring in:** `run()` previously just set `self.running = True` and returned immediately — not an actual loop, so using it as a thread target would have done nothing. Turned into a real 2s-cadence `monitor_ecs()` loop; added `start()`/`stop()`/`get_status()`/`get_ecs_control()` singleton. See commit `18cba09`. |
| ~~`AirframeSystemManager`~~ | `Systems/airframeSystemManagement/airframeControl.py` | **Wired into the live boot sequence (August 2026).** Previously fixed but unwired (busy-loop `run()`, commit `50e0686`); now started via a new `get_airframe_system_manager()` singleton, same `threading.Event` start()/stop() pattern as `HydraulicSystemController`. The pre-existing unconditional `control_landing_gear("deploy")` call every tick was left as-is — out of scope for a wiring-only change. See commit `18cba09`. |
| `SoftwareConfigManager` | `Systems/configurationManagement/swConfigure.py` | **Wired into the live boot sequence (August 2026), deliberately without a background thread.** `run()` applies one specific hardcoded historical data load ("20191015") as a one-shot demo; looping that on a timer would "re-apply" the same update forever, which isn't realistic config-management behavior. `get_software_config_manager()` singleton constructs the class (parsing `swcmConfig.xml`, populating the component/data-load inventory) and is registered in `system_manager.py`'s components, but `display_component_status()`/`display_data_loads()`/`perform_update()` remain on-demand-only, not run automatically. See commit `18cba09`. |
| ~~`FlightDataMonitoring`~~ | `Systems/flightDataMonitoring/fdmControl.py` | **Wired into the live boot sequence (August 2026).** `run()`'s original one-shot demo (`collect_data()` once, then `eject_storage(1)`/`eject_storage(2)`) was left unchanged for standalone use, but wiring it in as a repeating loop as-is would have "ejected" both recorder storage cards every few seconds forever, which doesn't reflect real flight-data-recorder behavior (storage ejection is a ground-crew action). Added a separate `_record_loop()` that repeats only `collect_data()` on a 2s cadence; `eject_storage()` remains available on demand. New `get_flight_data_monitoring()` singleton. See commit `18cba09`. |
| ~~`FlightManagementFitness`~~ | `Systems/fmFitness/fmStatus.py` | **Wired into the live boot sequence (August 2026).** Previously fixed but unwired (missing-sleep busy-loop bug, commit `50e0686`); now started via a new `get_flight_management_fitness()` singleton, same `threading.Event` start()/stop() pattern as the other newly-wired monitors. See commit `18cba09`. |
| ~~`Systems/powerManagement/batts/*.py`, `Systems/powerManagement/thermalManagement/**/*.py`, `Systems/engineManagement/thrustManagement/thrustManagement.py`, `Systems/engineManagement/fuelManagement/fuelTransfer.py`, `fuelMonitor.py`~~ | (various) | **Implemented (August 2026), then wired into the live boot sequence one round later.** All 10 were genuinely empty (1-line comment-only placeholders) — implemented first: `Battery` base class + `MainBattery`/`EmergencyBattery`/`APUBattery`/`MissionEquipmentBattery` (+ bank) in `batts/`; `ActiveDissipationUnit`/`CoolingSystem`/`HVACSystem` in `thermalManagement/`; `ThrustManagementSystem` (multi-engine thrust coordination + PI autothrottle); `FuelMonitor` (consumption tracking, low/critical-fuel alerts, leak-detection heuristic) and `FuelTransferManager` (rule-based inter-tank balancing) built on top of `FuelSystem` (`fuelControl.py`). **Now wired in (August 2026, following round):** the battery/`CoolingSystem`/`HVACSystem` classes are owned and driven by `PowerManagementSystem` (see above); `FuelMonitor`/`FuelTransferManager` are owned and driven by `FuelManagementSystem` (see above); `ThrustManagementSystem` got its own `get_thrust_management_system()` singleton (2Hz background loop, drives autothrottle off live FMS airspeed when engaged) and is started directly from `system_manager.py` Phase 3, registering a twin-engine ("Engine 1"/"Engine 2") configuration matching `FuelManagementSystem.setup_aircraft()`'s existing precedent — no other source in this codebase specifies B20SS engine count. A heat-load scaling bug was found and fixed while wiring `CoolingSystem` into `PowerManagementSystem` (see above); see commit `2603419` for both this and the `fuel_tanks` table fix. While in this neighborhood a round earlier, also fixed `Systems/enviornmentalControlSystem/climate/climateControl.py` and `oxygenGenerationsys/oxygenControl.py`, which imported a `FMOFP.local_messaging.Messaging.Messaging` class that doesn't exist anywhere in the codebase (superseded long ago by the `local_messaging` routing/handler system) and called `sys_logger(name)` as a constructor when only `get_logger()` exists — both raised `ModuleNotFoundError` on every import, confirmed live, making them entirely unreachable regardless of the wiring question. Fixed to the standard `get_logger()` pattern used everywhere else. Full CI-equivalent suite and a full boot+SIGTERM cycle both re-run clean after all changes, both rounds. See commits `492429f` and `2603419`. |

For contrast, subsystems confirmed both wired-in *and* correct this round: `FlightSurfaceService` (`Systems/flightSurfaces/flightSurfaceService.py`, started from `system_manager.py`) — full control surface (ailerons/elevators/rudder/flaps/slats/speedbrake/trim) exercised live, zero errors; `BuiltInTestController` (`Systems/builtInTestSystems/bitControl.py`) and `LRUStatusMonitor` (`Systems/avionics/hardwareHealth/LRUstatus.py`, both reached via `eicas.py`) re-verified live, correctly reporting FAIL/FAULT status for dependent systems not running in isolation (expected behavior, not a bug).

**Update (August 2026, two following rounds — "continue to build this project out"):** `PowerManagementSystem`, `FuelManagementSystem`, `ThrustManagementSystem` (commit `2603419`), and then `HydraulicSystemController`, `AirframeSystemManager`, `ECSControl`, `FlightDataMonitoring`, `FlightManagementFitness`, and `SoftwareConfigManager` (commit `18cba09`) have all moved from this table into the wired-in set above. `ElectricalPowerController` and `FuelSystemController` are the only two subsystems in this section still deliberately unwired — both are duplicate implementations superseded by `PowerManagementSystem`/`FuelManagementSystem` respectively, kept unwired on purpose to avoid two competing simulations of the same aircraft system. As of commit `18cba09`, every genuinely-standalone subsystem this audit found has been wired into `system_manager.py`'s boot sequence.

**Update (August 2026, production-readiness re-analysis round, post-test-coverage commit).** A repo-wide dead-code sweep (every top-level class definition grepped for references outside its own file, ~690 classes scanned) found three more items worth recording:

- ~~`ClimateControl` (`Systems/enviornmentalControlSystem/climate/climateControl.py`) and `OxygenControl` (`oxygenGenerationsys/oxygenControl.py`)~~ — **Wired into `ECSControl` (August 2026).** Both were fixed for the `ModuleNotFoundError` import bug in an earlier round (see the note in the row above), but that fix only made them constructible — nothing ever actually instantiated them, confirmed by this round's sweep. They're complementary to, not duplicates of, `ECSControl`'s existing `get_temperature()`/`get_pressure()`/`get_air_quality()` readings (which remain a documented stub with no real sensor source): `ClimateControl` tracks genuinely mutable temperature/humidity state, and `OxygenControl` models cabin oxygen generation, a concern `ECSControl` didn't cover at all. Now owned by `ECSControl` the same way `PowerManagementSystem` owns its battery/cooling/HVAC sub-components — `monitor_ecs()` drives `oxygen.generate_oxygen()` and `climate.adjust_temperature()` every 2s tick, and `get_status()` exposes `climate`/`oxygen` sub-dicts. Verified live: boot log shows `"[ECS] Environmental Control System started"` followed by repeating `"Generating oxygen at 1.0 L/min"` lines on cadence, zero errors over a 35s run.
- `MissionManagementSystem` (`Systems/missionPlanning/missionControl.py`) — a third instance of the same "competing duplicate implementation" pattern as `ElectricalPowerController`/`FuelSystemController` above, not previously documented in this table. A full, separate 355-line mission-planning implementation (waypoints/targets/threats/route optimization, its own `start()`/`stop()`/`run()`) distinct from `MissionService` (`missionService.py`, already wired in and verified this session). Confirmed live: constructs cleanly, methods present and callable. **Deliberately left unwired** — wiring it in alongside the already-live `MissionService` would create two competing mission-planning simulations, the exact outcome `ElectricalPowerController`/`FuelSystemController` were kept unwired to avoid.
- `FuturisticPFD`/`FuturisticMFD` (`Interfaces/userInterface/displays/futuristic_pfd.py`/`futuristic_mfd.py`) and `NodeBasedPFD` (`node_based_pfd.py`) — confirmed **not a bug, deliberately deprecated**. `pfd_display_factory.py` imports `FuturisticPFD` but its `create_display()` only branches on `"holographic"` vs. default `"standard"` — no `"futuristic"` branch exists, so the import is unreachable in practice; `mfd_display_factory.py` doesn't even import `FuturisticMFD`. Confirmed via `theme_manager.py`: `get_available_display_types()` explicitly returns only `["standard", "holographic"]`, and two other files carry explicit removal comments — `mfd.py`'s display-mode options list is annotated `# Removed "futuristic"`, and `settings_panel.py`'s default was "Changed default from 'futuristic' to 'modern'". This reads as an intentional product decision to retire the futuristic theme, with the now-orphaned display classes simply never deleted — a different category from the wiring gaps elsewhere in this table (those were *never-finished* features; this was a *finished-then-removed* one). No action taken on `FuturisticPFD`/`FuturisticMFD`; documented for the record. **Update:** `node_based_pfd.py` (`NodeBasedPFD`) was subsequently **deleted** as baseline hygiene. It was re-confirmed dead — no module imports it, `pfd_display_factory.py` has no branch for it, `get_available_display_types()` does not list it, and no test, packaging manifest or tooling references it — and it carried a near-verbatim duplicate of `PrimaryFlightDisplay`’s rendering methods (its `draw_altitude_tape` differed from the live one by a single added comment), which is a maintenance trap for anyone who edits the wrong copy. `FuturisticPFD`/`FuturisticMFD` were left in place: `pfd_display_factory.py` still imports `FuturisticPFD`, so removing it would require a factory change and was out of scope for that cleanup.

**Also found and fixed this round (dependency completeness, not a wiring gap):** `Utils/debug/userCLI.py` does a top-level `import click`, and `core/system_manager.py` imports `get_user_cli` from it at module load time (used unconditionally in the live boot sequence for the `UserCLI_Control`/`Input`/`Processing`/`Output` components) — but `click` was missing from both `requirements.txt` and `install.py`'s `REQUIRED_PACKAGES`. The exact same class of gap as the scipy entry in `requirements.txt`, just one level more severe: a scipy-less install silently loses one feature, but a click-less install can't import `system_manager.py` at all, so the entire application fails to start. Every boot test this entire session passed silently only because this sandbox happened to already have click 8.4.1 present (a transitive dependency of some other preinstalled tool), masking the gap. Added `click>=8.0,<9.0` to `requirements.txt` and `("click", "click")` to `install.py`'s `REQUIRED_PACKAGES`.

A general note from this audit: the previous "stub" claims for 4 subsystems were wrong
because they were taken from this document rather than verified against the code, and this
document itself was stale. Any future status claims in this file should be re-verified
against actual code/runtime behavior before being trusted, including everything above this
note that predates the August 2026 audit.

---

## 15. Next Steps (Recommended Priority Order)

### Immediate (Bug fixes) — ✅ All completed

1. ~~**Clean up debug logging**~~ — **Done.** All 26 `[VIL_FLOW_DEBUG]` / `[PRECIPITATION_FLOW_DEBUG]` / `[TRANSFER_DATA_DEBUG]` `logger.error()` calls downgraded to `logger.debug()`.
2. ~~**Weather radar 1553B data path**~~ — **Done.** Removed the re-entry loop in `PrecipitationResponseService` and `VILResponseService`. Bridge push is now the canonical delivery path; 1553B chain no longer forms a loop.
3. ~~**CLI status request**~~ — **Done.** `_handle_status_command()` implemented in `userCLI.py`, covering system state, all 5 radars, FMS, FCS, Nav, Comms, and 1553B bus.

### Short-term (Feature completion) — ✅ Done

4. ~~**Storm cell display integration**~~ — Already wired in both `_process_surveillance_data()` and the main VIL path via `push_cells_data()`.
5. ~~**Wind shear + turbulence display overlays**~~ — **Done.** `push_windshear_data()` / `push_turbulence_data()` added to bridge; `_draw_windshear_overlay()` / `_draw_turbulence_overlay()` added to `WeatherRadarDisplay`.
6. ~~**Defensive systems implementation**~~ — **Done, predates this correction.** `Systems/defensiveSys/` already has RWR contact tracking, chaff/flare countermeasures, and ECM mode management; confirmed working via live run in the August 2026 audit (see Known Issues).
7. ~~**Sensor management**~~ — **Done, predates this correction.** `Systems/sensorManagement/` already has active/passive sensor managers and cross-sensor fusion; had a severe LIDAR bug that's now fixed (see Known Issues).

### Medium-term

8. ~~**Cross-radar display integration**~~ — **Done, predates this correction.** `tsd.py` already pulls `RadarDataFusion.get_fused_tracks()`; `eicas.py` already pulls `get_threat_tracks()` for fused-hostile-count alerts. This item was stale.
9. ~~**Mission planning completion (UI only)**~~ — **Done, verified end-to-end (August 2026).** Populated real waypoints, a designated target, and a mission phase via `MissionService`, then rendered MFD's `draw_mission_page()` across all three sub-tabs (ROUTE / TARGETS / OOB) and `TacticalSituationDisplay.paint_display()` — all rendered without error and reflected the live data (2 waypoints, 1 OOB target). One methodology note from this verification, not a product bug: calling `MissionService` methods from a process that has constructed a `QApplication` but is not running its event loop (`app.exec()`) can hang — some part of this chain uses a cross-thread Qt signal/slot that only gets delivered once the event loop is actually spinning. Confirmed this resolves cleanly the moment the call happens from inside a running event loop, which is how every real caller (message handlers, UI callbacks) invokes it in production — not reachable in normal operation, just a trap for future ad-hoc test scripts that construct `QApplication` without running it.
10. ~~**Performance optimisation**~~ — **Done (August 2026).** Spatial partitioning is implemented in the rendering engine. Profiled live: see Section 16 (Performance Targets) — display refresh, CPU per radar, and memory per radar all measured directly and pass their targets with large margin. This item was stale relative to Section 16, which had already been updated; corrected here so the two sections agree.
11. ~~**Linux / macOS support**~~ — **Done, predates this correction.** `install.py` already branches on `platform.system()`: bundled wheels on Windows, plain `pip install` from PyPI on Linux/macOS. CI itself runs the full test suite on `ubuntu-latest` across Python 3.10-3.14, which is a live proof this path works. This item was stale.

### Long-term

12. ~~**Hardware interface**~~ — **Done to the limit of what software alone can do (August 2026 completion pass).** `MIL_STD_1553B/bus_adapter.py` is now the single, documented transport integration point: a `BusAdapter` interface with three implementations — `SocketBusAdapter` (default; byte-for-byte the previous inline TCP behavior of `BC_socket`/`RT_socket`, which now delegate all wire transfers to the adapter), `LoopbackBusAdapter` (in-process test transport), and `HardwareBusAdapter` (real-card template: delegates to a vendor driver object registered via `register_hardware_driver()`, with the driver contract documented on the class; fails instructively when none is registered). Selection is per role via `busAdapterConfig.xml` (absent/unparseable file ⇒ socket defaults, so stock checkouts are behavior-identical). Covered by `Tests/test_bus_adapter.py` (24 assertions, in CI via `run_all_tests.py`). The remaining step — writing and validating an actual vendor driver (Alta/DDC/Abaco/AIM SDK) — inherently **requires a physical 1553B interface card on a physical bus** and stays open until hardware exists; no further code can be honestly written for it in a pure-software environment.
12a. ~~**Debug CLI test menu cleanup**~~ — **Done (August 2026 completion pass).** The interactive `test` menu had already been cut to the 9 entries whose `FMOFP.Tests.*` modules exist; this pass deleted the ~16 dead handler methods (`weather_radar_display_visual_test`, `vil_display_flow_test`, `precipitation_display_flow_test`, `echo_top_display_flow_test`, `weather_radar_display_test`, `weather_radar_end_to_end_test`, `precipitation_system_test`, `vil_system_test`, the four `*_radar_mode_change` handlers, `weather_radar_node_test`, `weather_radar_visual_test`, `test_display_individual_mode`, `TestWeatherRadarModeChange`) that referenced nonexistent test modules and were unreachable from the menu (confirmed no other callers repo-wide). `userCLI.py` shrank ~370 lines; every remaining `_import_test_module()` target exists in `Tests/`. The helper's graceful-failure path is kept as defense-in-depth against future menu/Tests drift.
13. ~~**Automated CI**~~ — **Done.** `.github/workflows/ci.yml` added and pushed to `origin/main`.
14. ~~**Scenario engine**~~ — **Done, predates this correction — item was stale.** `Interfaces/scenarios/scenarioEngine.py` is a fully-implemented runtime: `load()`/`start()`/`stop()`, a background dispatch thread, and 6 event injectors. Confirmed live: 4 of 5 injectors (threat, weather, waypoint, phase) call real, correctly-signatured methods; `_inject_radar_contact` was calling a nonexistent `RadarDataFusion.ingest_targeting_data()` (silently swallowed by its own broad except) — found and fixed in the August 2026 audit by writing scenario contacts into a radar's `current_targets` table instead, matching `RadarDataFusion`'s actual pull-based design. `_inject_failure` was log-only until the August 2026 completion pass — no failure-injection state existed for it to feed. It is now functional: `LRUStatusMonitor` (Systems/avionics/hardwareHealth/LRUstatus.py) gained a forced-fault override (`force_fault()` / `clear_forced_fault()` / `get_forced_faults()`) that `_poll_lru()` honors on every poll tick — exactly the hook whose absence the earlier audit documented (any directly-set `.health` was overwritten a fraction of a second later). Scenario `system_failure` events now resolve their `<system>` value against the LRU catalogue (lru_id / system_key / unique name substring; non-catalogued names become scenario-declared entries so instructor scenarios like `engine_vibration`/`fuel_transfer` still register), honor `<state>` FAULT/DEGRADED/OFFLINE, and support `action=clear`/`clear_all` events; injected faults persist in `get_all()`/`get_faults()`/`overall_health()` until cleared. Covered end-to-end (including every event in `failureScenario.xml` through the real dispatcher) by `Tests/test_scenario_failure_injection.py` (29 assertions, in CI via `run_all_tests.py`).

---

## 16. Performance Targets

| Metric | Target | Notes |
|--------|--------|-------|
| Display refresh | 60 Hz | **Profiled live (August 2026).** `FMOFP/Tests/performance_profile.py` already existed in the repo but had apparently never been run — running it now shows raw `paint_display()` cost is nowhere near the bottleneck: PFD mean 1.98 ms, MFD mean 0.78 ms, both against a 16.7 ms/60 Hz budget (all 7 profiled displays PASS with large margin; even the heaviest, Holographic MFD, is 34.9 ms against its own 100 ms/10 Hz budget). The previously-documented "PFD 20 Hz actual / MFD 10 Hz actual" figures could not be reproduced from render cost and appear to have been a stale, never-revalidated estimate rather than a real ceiling — no code fix was needed, since nothing is actually slow. |
| Message latency | < 10 ms | Async handler with 4 workers |
| CPU per radar | < 40% | **Profiled live (August 2026).** Radars in this codebase are reactive/message-driven, not continuously-running simulation threads — `radar.start()` only flips a running flag; target generation and processing (`_update_targets` / `_run_target_processing`) only execute inside the handler for an incoming track-request message. Benchmarked that hot path directly: ~0.08 ms mean per poll, which is <1% CPU even at a sustained 60 Hz polling rate — far under the 40% target. |
| Memory per radar | < 256 MB | **Profiled live (August 2026).** All 5 radars + `RadarDataFusion` together use ~65 MB RSS after `initialize_radars()` — comfortably under 256 MB even if that whole figure were attributed to a single radar. |
| Database workers | ≤ 20 total | Enforced by `dbConfig.xml` |
| Radar query rate | 200 / 120 s | High-performance config |
| Display query rate | 200 / 60 s | Real-time config |
| Rate limiting | 10 req/s | AsyncMessageHandler |

---

## 16.1 Documentation Status (August 21, 2026)

**User manual updated to v1.2 / FMOFP 1.2.0.** The manual had accumulated
stale status markers — most consequentially it still described the weather
radar → display integration as a 🐛 known issue in which "1553B
communication prevents data display", a defect fixed some time ago. Every
marker was re-checked against the code and, where possible, against a live
run. Corrections made:

| Claim in the manual | Reality | Evidence |
|---|---|---|
| Weather radar data cannot reach displays (🐛, ~12 markers across 5 files) | Operational | 104 bridge deliveries + 104 matching display updates in a live 50 s run |
| Other radars' display integration "not yet implemented" | Operational for all 5 radars | `test_bridge_and_coordinator` covers every push function |
| MFD Systems page is a static placeholder (❌) | Live | `draw_systems_page` reads ECU, GCAS, PerformanceMonitor |
| MFD Communications page is hardcoded text (❌) | Live | `draw_comms_page` reads `CommsService` |
| "No dedicated tactical display" (❌) | TSD exists | `tsd.py` draws `RadarDataFusion.get_fused_tracks()`, covered by `test_displays_headless` |
| Wind shear / turbulence "planned" (❌) | Implemented | `WindShearProcessor`, `_process_turbulence_data`, display overlays |
| Mission planning integration "planned" (❌) | Implemented | `draw_mission_page` with ROUTE/TARGETS/OOB |
| Python 3.9+ (unqualified) | 3.9 is the installer minimum; CI verifies 3.10–3.14 | `install.py` `MIN_PYTHON` vs `ci.yml` matrix |

Markers went from 20 active 🐛 to 0 (the 8 remaining occurrences are the
status legend and explicit historical notes), and ❌ from 46 to 33 — the
remainder verified as genuinely unimplemented (MFD nav page, MFD weapons
page, and forward-looking "required features" lists).

Content added: File 11 §11.1.1 documenting the bus adapter layer and the
hardware driver contract; File 13 §13.5 rewritten around the 15-suite
aggregating runner; Appendix C.1.3 for `busAdapterConfig.xml`; scenario
`system_failure` event parameters; and a version history in the legal
chapter. Deliberately left alone: the MFD nav and weapons pages, which are
still placeholders, and whose ❌ markers are accurate.

---

## 17. Daily Progress Log

> **Provenance note (added August 21, 2026).** The repository history was
> squashed to a single "Initial commit" when it was re-published, so **every
> commit hash cited in this document below (and in the Known Issues tables
> above) refers to the retired pre-squash history and no longer resolves in
> this repository.** Verified: `7dc02ee`, `094e50b`, `8f37be6`, `e3419f5`,
> `5e09886`, `ff72980` and their siblings all fail `git cat-file -e` here.
> They are retained deliberately — the audit narrative is more useful with
> them than without — but treat them as *historical references*, not as
> commits you can check out. Only commits made after the re-publish
> (`1b8090f` onward) are resolvable. If the pre-squash history is still
> archived elsewhere, keeping that clone/branch is what makes these
> references followable; otherwise this document is their only record.


### 2025-02-03
- Created consolidated working notes
- Analysed existing implementations
- Designed integration strategy
- Planned implementation phases

### 2025-02-08
- VIL implementation: `vil_data_handler.py`, `vil_response_service.py`, `WeatherRadarVILData` class
- Identified VIL database transaction issues (raw SQL, double storage, improper queue completion)
- Designed fix plan: use DBM `create_table`, fix queue completion, add timeout handling

### 2025-02-08 → (subsequent commits, reconstructed from git log)

| Commit | Change |
|--------|--------|
| `7dc02ee` | Initial commit |
| `04a39c1` | Enhanced VILResponseService + Radar-to-Display Bridge introduced |
| `ec2b86f` | GPS and Storm Cell Tracking refactor |
| `79b4b99` | FMS and Radar completion message handler refactor |
| `62c26b3` | Cross-radar data fusion layer (`RadarDataFusion`) |
| `a089dcc` | SAR, Targeting, TFR, Weather radar processors implemented |
| `49d0f02` | Comprehensive test suites: bridge, display, installer |
| `6abf254` | Operational status enhancements, radar/FMS feature improvements |
| `c8dd198` | Stores Management System (SMS) display added |
| `c366b23` | EICAS and TSD display modules added |
| `67121d6` | Automated installer (`install.py`) |
| `93b0b8a` | Radar management refactor for data handling and display integration |
| `c23751c` | User manual updated to reflect operational radar status |

---

*Document generated by full codebase audit — June 2026*
